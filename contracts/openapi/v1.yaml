openapi: 3.1.0
info:
  title: LLPlayerNext Local API
  version: 1.0.0
  description: Versioned loopback-only application-service adapter. Playback control is excluded.
servers:
  - url: http://127.0.0.1:{port}
    variables:
      port:
        default: "0"
security:
  - bearerAuth: []
paths:
  /v1/health:
    get:
      security: []
      operationId: health
      responses:
        "200":
          description: Service health and API version
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Health"
  /v1/media:
    post:
      operationId: registerMedia
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterMedia"
      responses:
        "200":
          description: Idempotently registered media
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MediaItem"
        default:
          $ref: "#/components/responses/Error"
  /v1/lltimeline/import:
    post:
      operationId: importLLTimeline
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LLTimelineDocument"
      responses:
        "200":
          description: Imported LLTimeline JSON v1 resource document
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubtitleTrack"
  /v1/media/{media_id}:
    parameters:
      - $ref: "#/components/parameters/MediaId"
    get:
      operationId: readMedia
      responses:
        "200":
          description: Media metadata
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MediaItem"
        default:
          $ref: "#/components/responses/Error"
  /v1/media/{media_id}/progress:
    parameters:
      - $ref: "#/components/parameters/MediaId"
    get:
      operationId: readProgress
      responses:
        "200":
          description: Last saved position
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Progress"
        default:
          $ref: "#/components/responses/Error"
    put:
      operationId: updateProgress
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Progress"
      responses:
        "200":
          description: Saved position
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Progress"
        default:
          $ref: "#/components/responses/Error"
  /v1/media/{media_id}/subtitles:
    parameters:
      - $ref: "#/components/parameters/MediaId"
    post:
      operationId: importSubtitle
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ImportSubtitle"
      responses:
        "200":
          description: Complete normalized subtitle timeline
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubtitleTrack"
        default:
          $ref: "#/components/responses/Error"
  /v1/subtitles/{track_id}:
    parameters:
      - name: track_id
        in: path
        required: true
        schema: { type: string }
    get:
      operationId: readSubtitle
      responses:
        "200":
          description: Complete normalized subtitle timeline
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SubtitleTrack"
        default:
          $ref: "#/components/responses/Error"
  /v1/subtitles/{track_id}/export:
    get:
      operationId: exportSubtitle
      parameters:
        - { name: track_id, in: path, required: true, schema: { type: string } }
        - { name: format, in: query, schema: { type: string, default: srt } }
      responses:
        "200": { description: Generated SRT subtitle }
  /v1/pronunciation/providers:
    get:
      operationId: pronunciationProviders
      responses:
        "200": { description: Provider-neutral pronunciation capability registry }
  /v1/pronunciation/lookup:
    get:
      operationId: pronunciationLookup
      parameters:
        - { name: word, in: query, required: true, schema: { type: string } }
      responses:
        "200": { description: Canonical word pronunciation and variants }
  /v1/pronunciation/analyze-sentence:
    post:
      operationId: analyzeSentencePronunciation
      responses:
        "200": { description: Sentence pronunciation, token mapping, and rule hints }
  /v1/pronunciation/rules:
    get:
      operationId: pronunciationRules
      responses:
        "200": { description: Deterministic rule analyzer metadata and disclaimer }
  /v1/subtitles/{track_id}/pronunciation:
    get:
      operationId: trackPronunciation
      responses:
        "200": { description: Cached or generated pronunciation analyses for a track }
  /v1/subtitles/{track_id}/pronunciation-analysis:
    post:
      operationId: generateTrackPronunciation
      responses:
        "200": { description: Generated pronunciation analyses for a track }
  /v1/subtitles/{track_id}/word-timings:
    get:
      operationId: trackWordTimings
      responses:
        "200":
          description: Word timings with source and provider provenance
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/WordTiming" }
    post:
      operationId: generateTrackWordTimings
      responses:
        "200":
          description: Generated word timings with safe estimated fallback
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/WordTiming" }
  /v1/subtitles/{track_id}/word-timelines:
    get:
      operationId: trackWordTimelines
      responses:
        "200":
          description: Versioned word timeline resources for a subtitle track
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/WordTimeline" }
    post:
      operationId: createTrackWordTimeline
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateWordTimeline"
      responses:
        "200":
          description: Created word timeline resource
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WordTimeline"
  /v1/subtitles/{track_id}/lltimeline/export:
    get:
      operationId: exportTrackLLTimeline
      responses:
        "200":
          description: Exportable LLTimeline JSON v1 resource document
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LLTimelineDocument"
  /v1/word-timelines/{timeline_id}:
    get:
      operationId: wordTimeline
      responses:
        "200":
          description: Versioned word timeline resource
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WordTimeline"
  /v1/word-timelines/{timeline_id}/activate:
    post:
      operationId: activateWordTimeline
      responses:
        "200":
          description: Activated word timeline resource
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WordTimeline"
  /v1/word-timelines/{timeline_id}/archive:
    post:
      operationId: archiveWordTimeline
      responses:
        "200":
          description: Archived word timeline resource
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WordTimeline"
  /v1/word-timelines/{timeline_id}/export:
    get:
      operationId: exportWordTimeline
      responses:
        "200":
          description: Exportable word timeline resource
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WordTimeline"
  /v1/subtitles/{track_id}/word-timing-diagnostics:
    get:
      operationId: trackWordTimingDiagnostics
      responses:
        "200": { description: Developer-facing inter-word gaps with timing source and provider provenance }
  /v1/subtitles/{track_id}/chunk-partitions:
    get:
      operationId: trackChunkPartitions
      responses:
        "200": { description: Product-facing complete chunk partitions for a subtitle track }
  /v1/subtitles/{track_id}/chunk-diagnostics:
    get:
      operationId: trackChunkDiagnostics
      responses:
        "200": { description: Developer-facing selected and rejected chunk boundary evidence }
  /v1/chunk/providers:
    get:
      operationId: chunkProviders
      responses:
        "200": { description: Optional learned chunk evidence providers, revisions, runtimes, and licenses }
  /v1/speech/jobs:
    get:
      operationId: speechBatchJobs
      responses:
        "200": { description: In-memory speech enhancement batch jobs }
    post:
      operationId: createSpeechBatchJob
      responses:
        "200": { description: Queued non-blocking pronunciation or word timing job }
  /v1/speech/jobs/{job_id}:
    get:
      operationId: speechBatchJob
      responses:
        "200": { description: Speech enhancement batch job status }
  /v1/speech/jobs/{job_id}/cancel:
    post:
      operationId: cancelSpeechBatchJob
      responses:
        "200": { description: Cancelled speech enhancement batch job }
  /v1/speech/jobs/{job_id}/retry:
    post:
      operationId: retrySpeechBatchJob
      responses:
        "200": { description: Retried speech enhancement batch job }
  /v1/lexical-entries:
    get:
      operationId: listLexicalEntries
      parameters:
        - { name: language, in: query, schema: { type: string, default: en } }
        - { name: kind, in: query, schema: { enum: [word, phrase] } }
        - { name: status, in: query, schema: { $ref: "#/components/schemas/WordStatus" } }
        - { name: search, in: query, schema: { type: string } }
        - { name: limit, in: query, schema: { type: integer, minimum: 1, maximum: 200 } }
        - { name: offset, in: query, schema: { type: integer, minimum: 0 } }
      responses:
        "200":
          description: Unified word and phrase learning assets
          content:
            application/json:
              schema: { type: array, items: { $ref: "#/components/schemas/LexicalEntryDetails" } }
    put:
      operationId: upsertLexicalEntry
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/UpsertLexicalEntry" }
      responses:
        "200":
          description: Updated lexical learning asset
          content:
            application/json:
              schema: { $ref: "#/components/schemas/LexicalEntryDetails" }
  /v1/lexical-entries/{id}:
    get:
      operationId: lexicalEntryDetails
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        "200":
          description: Lexical asset details
          content:
            application/json:
              schema: { $ref: "#/components/schemas/LexicalEntryDetails" }
  /v1/lexical-normalization:
    post:
      operationId: normalizeLexical
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/NormalizeLexicalRequest" }
      responses:
        "200":
          description: Versioned lexical normalization result
          content:
            application/json:
              schema: { $ref: "#/components/schemas/LexicalNormalization" }
  /v1/lexical-normalization/correct:
    post:
      operationId: correctLemma
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CorrectLemmaRequest" }
      responses:
        "200":
          description: Persisted user lemma correction
          content:
            application/json:
              schema: { $ref: "#/components/schemas/LexicalNormalization" }
        "409": { $ref: "#/components/responses/Error" }
  /v1/sentences/{sentence_id}/phrase-candidates:
    get:
      operationId: phraseCandidates
      parameters:
        - { name: sentence_id, in: path, required: true, schema: { type: string } }
      responses:
        "200":
          description: Phrase candidates requiring user confirmation
          content:
            application/json:
              schema: { type: array, items: { $ref: "#/components/schemas/PhraseCandidate" } }
  /v1/learning-resources:
    get:
      operationId: learningResources
      responses:
        "200":
          description: Offline learning resource catalog
          content:
            application/json:
              schema: { type: array, items: { $ref: "#/components/schemas/LearningResource" } }
  /v1/learning-resources/{id}/install:
    post:
      operationId: installLearningResource
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        "200":
          description: Installed and verified learning resource
          content:
            application/json:
              schema: { $ref: "#/components/schemas/LearningResource" }
  /v1/learning-resources/{id}:
    delete:
      operationId: removeLearningResource
      parameters:
        - { name: id, in: path, required: true, schema: { type: string } }
      responses:
        "200":
          description: Removed learning resource
          content:
            application/json:
              schema: { $ref: "#/components/schemas/LearningResource" }
  /v1/subtitle-search:
    post:
      operationId: searchSubtitles
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SubtitleSearchRequest" }
      responses:
        "200":
          description: Subtitle provider search results
          content:
            application/json:
              schema: { type: array, items: { $ref: "#/components/schemas/SubtitleSearchResult" } }
        "401": { $ref: "#/components/responses/Error" }
        "429": { $ref: "#/components/responses/Error" }
  /v1/subtitle-search/download:
    post:
      operationId: downloadSubtitle
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/SubtitleDownloadRequest" }
      responses:
        "200": { description: Downloaded subtitle bytes }
  /v1/word-profiles:
    get:
      operationId: readWordProfile
      parameters:
        - name: language
          in: query
          required: true
          schema: { type: string }
        - name: lemma
          in: query
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Profile or null when not classified
          content:
            application/json:
              schema:
                anyOf:
                  - $ref: "#/components/schemas/WordProfile"
                  - type: "null"
        default:
          $ref: "#/components/responses/Error"
    put:
      operationId: updateWordProfile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateWordProfile"
      responses:
        "200":
          description: Updated profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WordProfile"
        default:
          $ref: "#/components/responses/Error"
  /v1/word-profiles/batch:
    post:
      operationId: readWordProfiles
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BatchWordProfiles"
      responses:
        "200":
          description: Existing profiles; absent lemmas are unclassified
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/WordProfile" }
        default:
          $ref: "#/components/responses/Error"
  /v1/word-observations:
    post:
      operationId: createWordObservation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateWordObservation"
      responses:
        "200":
          description: Context-specific listening observation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WordObservation"
        default:
          $ref: "#/components/responses/Error"
  /v1/vocabulary:
    get:
      operationId: listVocabulary
      parameters:
        - { name: language, in: query, schema: { type: string, default: en } }
        - { name: status, in: query, required: true, schema: { enum: [unknown_meaning, known_not_recognized, known_recognized] } }
        - { name: search, in: query, schema: { type: string } }
        - { name: limit, in: query, schema: { type: integer, default: 100, maximum: 200 } }
        - { name: offset, in: query, schema: { type: integer, default: 0 } }
      responses:
        "200":
          description: Dynamic vocabulary book
          content:
            application/json:
              schema: { type: array, items: { $ref: "#/components/schemas/WordDetails" } }
  /v1/vocabulary/export:
    get:
      operationId: exportVocabulary
      responses:
        "200":
          description: Versioned portable vocabulary assets
          content:
            application/json:
              schema: { $ref: "#/components/schemas/VocabularyAssetBundle" }
  /v1/vocabulary/import:
    post:
      operationId: importVocabulary
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/VocabularyAssetBundle" }
      responses:
        "200": { description: Assets merged }
  /v1/vocabulary/import-external:
    post:
      operationId: importExternalVocabulary
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ExternalVocabularyImport" }
      responses:
        "200":
          description: External word list import summary
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ExternalVocabularyImportSummary" }
  /v1/word-profiles/{profile_id}/details:
    get:
      operationId: wordDetails
      parameters:
        - { name: profile_id, in: path, required: true, schema: { type: string } }
      responses:
        "200":
          description: Profile, history and durable sources
          content:
            application/json:
              schema: { $ref: "#/components/schemas/WordDetails" }
  /v1/word-profiles/{profile_id}/learning-content:
    put:
      operationId: updateWordLearningContent
      parameters:
        - { name: profile_id, in: path, required: true, schema: { type: string } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                user_definition: { type: [string, "null"] }
                personal_note: { type: [string, "null"] }
      responses:
        "200":
          description: Updated learning content and word details
          content:
            application/json:
              schema: { $ref: "#/components/schemas/WordDetails" }
  /v1/media/{media_id}/availability:
    put:
      operationId: updateMediaAvailability
      parameters:
        - $ref: "#/components/parameters/MediaId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [availability]
              properties:
                availability: { enum: [available, missing, archived] }
      responses:
        "200":
          description: Updated media
  /v1/events:
    get:
      operationId: events
      responses:
        "200":
          description: Versioned server-sent domain event stream
          content:
            text/event-stream:
              schema:
                type: string
  /v1/dictionary:
    get:
      operationId: dictionaryLookup
      parameters:
        - name: language
          in: query
          required: true
          schema: { type: string }
        - name: lemma
          in: query
          required: true
          schema: { type: string }
      responses:
        "200":
          description: Aggregated results from registered dictionary providers
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DictionaryLookupBundle" }
        default:
          $ref: "#/components/responses/Error"
  /v1/sentences/{sentence_id}/diagnosis:
    parameters:
      - name: sentence_id
        in: path
        required: true
        schema: { type: string }
    get:
      operationId: diagnoseSentence
      responses:
        "200":
          description: Deterministic explainable sentence diagnosis
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SentenceDiagnosis"
        default:
          $ref: "#/components/responses/Error"
  /v1/transcription/providers:
    get:
      operationId: transcriptionProviders
      responses:
        "200": { description: Available transcription providers and runtimes }
  /v1/transcription/models:
    get:
      operationId: transcriptionModels
      responses:
        "200": { description: Model catalog and installation states }
  /v1/transcription/models/install:
    post:
      operationId: installTranscriptionModel
      responses:
        "200": { description: Model installation started }
  /v1/transcription/models/register-custom:
    post:
      operationId: registerCustomTranscriptionModel
      responses:
        "200": { description: Custom model registered }
  /v1/transcription/models/{model_id}/cancel-install:
    post:
      operationId: cancelTranscriptionModelInstall
      responses:
        "200": { description: Model installation cancelled }
  /v1/transcription/models/{model_id}:
    delete:
      operationId: deleteTranscriptionModel
      responses:
        "204": { description: Model removed }
  /v1/transcription/jobs:
    get:
      operationId: transcriptionJobs
      responses:
        "200": { description: Durable transcription jobs }
    post:
      operationId: createTranscriptionJob
      responses:
        "200": { description: Queued or idempotently reused transcription job }
  /v1/transcription/jobs/{job_id}:
    get:
      operationId: transcriptionJob
      responses:
        "200": { description: Durable transcription job }
  /v1/transcription/jobs/{job_id}/cancel:
    post:
      operationId: cancelTranscriptionJob
      responses:
        "200": { description: Cancelled transcription job }
  /v1/transcription/jobs/{job_id}/retry:
    post:
      operationId: retryTranscriptionJob
      responses:
        "200": { description: Retried transcription job }
  /v1/transcription/jobs/{job_id}/archive:
    post:
      operationId: archiveTranscriptionJob
      responses:
        "200": { description: Archived transcription job }
  /v1/phonetic-analysis/providers:
    get:
      operationId: phoneticAnalysisProviders
      responses:
        "200": { description: Experimental real-audio phonetic provider capabilities }
  /v1/phonetic-analysis/models:
    get:
      operationId: phoneticAnalysisModels
      responses:
        "200": { description: Phonetic model catalog, provenance, licensing, and state }
  /v1/phonetic-analysis/models/install:
    post:
      operationId: installPhoneticAnalysisModel
      responses:
        "200": { description: Updated phonetic model installation state }
  /v1/phonetic-analysis/models/register-custom:
    post:
      operationId: registerCustomPhoneticAnalysisModel
      responses:
        "200": { description: Registered custom phonetic model }
  /v1/phonetic-analysis/models/{model_id}/cancel-install:
    post:
      operationId: cancelPhoneticAnalysisModelInstall
      responses:
        "200": { description: Cancelled phonetic model installation }
  /v1/phonetic-analysis/models/{model_id}:
    delete:
      operationId: deletePhoneticAnalysisModel
      responses:
        "204": { description: Deleted phonetic model }
  /v1/phonetic-analysis/jobs:
    get:
      operationId: phoneticAnalysisJobs
      responses:
        "200": { description: Durable phonetic analysis jobs }
    post:
      operationId: createPhoneticAnalysisJob
      responses:
        "200": { description: Queued phonetic analysis job }
  /v1/phonetic-analysis/jobs/{job_id}:
    get:
      operationId: phoneticAnalysisJob
      responses:
        "200": { description: Durable phonetic analysis job }
  /v1/phonetic-analysis/jobs/{job_id}/cancel:
    post:
      operationId: cancelPhoneticAnalysisJob
      responses:
        "200": { description: Cancelled phonetic analysis job }
  /v1/phonetic-analysis/jobs/{job_id}/retry:
    post:
      operationId: retryPhoneticAnalysisJob
      responses:
        "200": { description: Retried phonetic analysis job }
  /v1/subtitles/{track_id}/phonetic-analyses:
    get:
      operationId: trackPhoneticAnalyses
      responses:
        "200": { description: Versioned detected-phone timelines and analyses }
  /v1/phonetic-analysis/{analysis_id}/findings:
    get:
      operationId: phoneticAnalysisFindings
      responses:
        "200": { description: Explainable findings for one phonetic analysis }
  /v1/phonetic-analysis/findings/{finding_id}/feedback:
    put:
      operationId: updatePhoneticFindingFeedback
      responses:
        "200": { description: Persisted user feedback for one finding }
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
  parameters:
    MediaId:
      name: media_id
      in: path
      required: true
      schema: { type: string }
  responses:
    Error:
      description: Structured error
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
  schemas:
    Health:
      type: object
      required: [status, api_version]
      properties:
        status: { const: ok }
        api_version: { const: 1 }
    RegisterMedia:
      type: object
      required: [path, fingerprint, title, kind]
      properties:
        path: { type: string }
        fingerprint: { type: string }
        title: { type: string }
        kind: { enum: [video, audio] }
        duration_ms: { type: [integer, "null"], minimum: 0 }
    MediaItem:
      type: object
      required: [id, path, fingerprint, title, kind, duration, availability, created_at_ms, updated_at_ms]
      properties:
        id: { type: string }
        path: { type: string }
        fingerprint: { type: string }
        title: { type: string }
        kind: { enum: [video, audio] }
        duration: { type: [integer, "null"], minimum: 0 }
        availability: { enum: [available, missing, archived] }
        created_at_ms: { type: integer, minimum: 0 }
        updated_at_ms: { type: integer, minimum: 0 }
    Progress:
      type: object
      required: [position_ms]
      properties:
        position_ms: { type: [integer, "null"], minimum: 0 }
    ImportSubtitle:
      type: object
      required: [path]
      properties:
        path: { type: string }
        language: { type: [string, "null"] }
    SubtitleToken:
      type: object
      required: [index, kind, text, normalized, start_char, end_char]
      properties:
        index: { type: integer, minimum: 0 }
        kind: { enum: [word, whitespace, punctuation, other] }
        text: { type: string }
        normalized: { type: [string, "null"] }
        start_char: { type: integer, minimum: 0 }
        end_char: { type: integer, minimum: 0 }
    SubtitleSentence:
      type: object
      required: [id, index, start, end, original_text, display_text, tokens]
      properties:
        id: { type: string }
        index: { type: integer, minimum: 0 }
        start: { type: integer, minimum: 0 }
        end: { type: integer, minimum: 0 }
        original_text: { type: string }
        display_text: { type: string }
        tokens:
          type: array
          items: { $ref: "#/components/schemas/SubtitleToken" }
    SubtitleTrack:
      type: object
      required: [id, media_id, fingerprint, language, source, sentences]
      properties:
        id: { type: string }
        media_id: { type: string }
        fingerprint: { type: string }
        language: { type: [string, "null"] }
        source: { type: string }
        sentences:
          type: array
          items: { $ref: "#/components/schemas/SubtitleSentence" }
    TimingSource:
      enum: [asr_reported, forced_aligned, estimated, user_adjusted]
    TimelineCreator:
      enum: [algorithm, user]
    TimelineStatus:
      enum: [candidate, active, archived]
    WordTiming:
      type: object
      required: [sentence_id, token_index, text, start_ms, end_ms, confidence, timing_source, provider_id, provider_version]
      properties:
        sentence_id: { type: string }
        token_index: { type: integer, minimum: 0 }
        text: { type: string }
        start_ms: { type: integer, minimum: 0 }
        end_ms: { type: integer, minimum: 0 }
        confidence: { type: [number, "null"], minimum: 0, maximum: 1 }
        timing_source: { $ref: "#/components/schemas/TimingSource" }
        provider_id: { type: string }
        provider_version: { type: string }
    WordTimeline:
      type: object
      required: [id, track_id, media_id, algorithm_id, algorithm_version, config_hash, parent_timeline_id, created_by, status, metrics_json, words, created_at_ms, updated_at_ms]
      properties:
        id: { type: string }
        track_id: { type: string }
        media_id: { type: string }
        algorithm_id: { type: string }
        algorithm_version: { type: string }
        config_hash: { type: string }
        parent_timeline_id: { type: [string, "null"] }
        created_by: { $ref: "#/components/schemas/TimelineCreator" }
        status: { $ref: "#/components/schemas/TimelineStatus" }
        metrics_json: { type: object, additionalProperties: true }
        words:
          type: array
          items: { $ref: "#/components/schemas/WordTiming" }
        created_at_ms: { type: integer, minimum: 0 }
        updated_at_ms: { type: integer, minimum: 0 }
    CreateWordTimeline:
      type: object
      required: [words]
      properties:
        algorithm_id: { type: [string, "null"] }
        algorithm_version: { type: [string, "null"] }
        config_hash: { type: [string, "null"] }
        parent_timeline_id: { type: [string, "null"] }
        created_by:
          anyOf:
            - $ref: "#/components/schemas/TimelineCreator"
            - type: "null"
        status:
          anyOf:
            - $ref: "#/components/schemas/TimelineStatus"
            - type: "null"
        metrics_json: { type: [object, "null"], additionalProperties: true }
        words:
          type: array
          items: { $ref: "#/components/schemas/WordTiming" }
    LLTimelineDocument:
      type: object
      required: [schema, metadata, segments, word_timelines, active_word_timeline_id, phone_timelines, active_phone_timeline_id, chunk_timelines, active_chunk_timeline_id, artifacts]
      properties:
        schema: { const: llplayer.timeline.v1 }
        metadata: { $ref: "#/components/schemas/LLTimelineMetadata" }
        segments:
          type: array
          items: { $ref: "#/components/schemas/LLTimelineSegment" }
        word_timelines:
          type: array
          items: { $ref: "#/components/schemas/WordTimeline" }
        active_word_timeline_id: { type: [string, "null"] }
        phone_timelines:
          type: array
          items: { type: object, additionalProperties: true }
        active_phone_timeline_id: { type: [string, "null"] }
        chunk_timelines:
          type: array
          items: { $ref: "#/components/schemas/LLTimelineChunkTimeline" }
        active_chunk_timeline_id: { type: [string, "null"] }
        artifacts:
          type: array
          items: { $ref: "#/components/schemas/LLTimelineArtifact" }
    LLTimelineMetadata:
      type: object
      required: [created_at_ms, generator, media, language, human_reviewed, extra]
      properties:
        created_at_ms: { type: integer, minimum: 0 }
        generator: { $ref: "#/components/schemas/LLTimelineGenerator" }
        media: { $ref: "#/components/schemas/LLTimelineMedia" }
        language: { type: [string, "null"] }
        human_reviewed: { type: boolean }
        extra: { type: object, additionalProperties: true }
    LLTimelineGenerator:
      type: object
      required: [id, version, mode]
      properties:
        id: { type: string }
        version: { type: string }
        mode: { type: string }
    LLTimelineMedia:
      type: object
      required: [id, fingerprint, path, title, duration_ms]
      properties:
        id: { type: string }
        fingerprint: { type: string }
        path: { type: [string, "null"] }
        title: { type: string }
        duration_ms: { type: [integer, "null"], minimum: 0 }
    LLTimelineSegment:
      type: object
      required: [id, index, start_ms, end_ms, text, display_text, tokens]
      properties:
        id: { type: string }
        index: { type: integer, minimum: 0 }
        start_ms: { type: integer, minimum: 0 }
        end_ms: { type: integer, minimum: 0 }
        text: { type: string }
        display_text: { type: string }
        tokens:
          type: array
          items: { $ref: "#/components/schemas/SubtitleToken" }
    LLTimelineChunkTimeline:
      type: object
      required: [id, word_timeline_id, algorithm_id, algorithm_version, created_by, status, chunks, metrics_json]
      properties:
        id: { type: string }
        word_timeline_id: { type: [string, "null"] }
        algorithm_id: { type: string }
        algorithm_version: { type: string }
        created_by: { $ref: "#/components/schemas/TimelineCreator" }
        status: { $ref: "#/components/schemas/TimelineStatus" }
        chunks:
          type: array
          items: { $ref: "#/components/schemas/LLTimelineChunk" }
        metrics_json: { type: object, additionalProperties: true }
    LLTimelineChunk:
      type: object
      required: [index, start_ms, end_ms, text, sentence_ids, word_refs, evidence]
      properties:
        index: { type: integer, minimum: 0 }
        start_ms: { type: integer, minimum: 0 }
        end_ms: { type: integer, minimum: 0 }
        text: { type: string }
        sentence_ids:
          type: array
          items: { type: string }
        word_refs:
          type: array
          items: { $ref: "#/components/schemas/LLTimelineWordRef" }
        evidence: { type: object, additionalProperties: true }
    LLTimelineWordRef:
      type: object
      required: [sentence_id, token_index]
      properties:
        sentence_id: { type: string }
        token_index: { type: integer, minimum: 0 }
    LLTimelineArtifact:
      type: object
      required: [kind, provider_id, provider_version, payload]
      properties:
        kind: { type: string }
        provider_id: { type: [string, "null"] }
        provider_version: { type: [string, "null"] }
        payload: { type: object, additionalProperties: true }
    WordStatus:
      enum: [unknown_meaning, known_not_recognized, known_recognized]
    LexicalSource:
      type: object
      required: [original_form, sentence_text, media_title, media_fingerprint, start_ms, end_ms]
      properties:
        media_id: { type: [string, "null"] }
        sentence_id: { type: [string, "null"] }
        original_form: { type: string }
        sentence_text: { type: string }
        media_title: { type: string }
        media_fingerprint: { type: string }
        start_ms: { type: integer, minimum: 0 }
        end_ms: { type: integer, minimum: 0 }
        token_start: { type: [integer, "null"], minimum: 0 }
        token_end: { type: [integer, "null"], minimum: 0 }
    UpsertLexicalEntry:
      type: object
      required: [language, kind, canonical_form, display_form]
      properties:
        language: { type: string }
        kind: { enum: [word, phrase] }
        canonical_form: { type: string }
        display_form: { type: string }
        status:
          anyOf:
            - $ref: "#/components/schemas/WordStatus"
            - type: "null"
        user_definition: { type: [string, "null"] }
        personal_note: { type: [string, "null"] }
        source:
          anyOf:
            - $ref: "#/components/schemas/LexicalSource"
            - type: "null"
    LexicalEntry:
      type: object
      required: [id, language, kind, canonical_form, normalized_form, display_form, status, user_definition, personal_note, normalization_provider, normalization_version, user_corrected, updated_at_ms, learning_updated_at_ms]
      properties:
        id: { type: string }
        language: { type: string }
        kind: { enum: [word, phrase] }
        canonical_form: { type: string }
        normalized_form: { type: string }
        display_form: { type: string }
        status:
          anyOf:
            - $ref: "#/components/schemas/WordStatus"
            - type: "null"
        user_definition: { type: [string, "null"] }
        personal_note: { type: [string, "null"] }
        normalization_provider: { type: string }
        normalization_version: { type: string }
        user_corrected: { type: boolean }
        updated_at_ms: { type: integer, minimum: 0 }
        learning_updated_at_ms: { type: integer, minimum: 0 }
    LexicalOccurrence:
      type: object
      required: [id, source_key, lexical_entry_id, original_form, sentence_text_snapshot, media_title_snapshot, media_fingerprint_snapshot, start_ms_snapshot, end_ms_snapshot, token_start, token_end, first_seen_at_ms, last_seen_at_ms, encounter_count]
      properties:
        id: { type: string }
        source_key: { type: string }
        lexical_entry_id: { type: string }
        media_id: { type: [string, "null"] }
        sentence_id: { type: [string, "null"] }
        original_form: { type: string }
        sentence_text_snapshot: { type: string }
        media_title_snapshot: { type: string }
        media_fingerprint_snapshot: { type: string }
        start_ms_snapshot: { type: integer, minimum: 0 }
        end_ms_snapshot: { type: integer, minimum: 0 }
        token_start: { type: [integer, "null"], minimum: 0 }
        token_end: { type: [integer, "null"], minimum: 0 }
        first_seen_at_ms: { type: integer, minimum: 0 }
        last_seen_at_ms: { type: integer, minimum: 0 }
        encounter_count: { type: integer, minimum: 1 }
    LexicalStatusHistory:
      type: object
      required: [id, lexical_entry_id, previous_status, new_status, changed_at_ms, change_source]
      properties:
        id: { type: string }
        lexical_entry_id: { type: string }
        previous_status: { type: [string, "null"] }
        new_status: { type: [string, "null"] }
        changed_at_ms: { type: integer, minimum: 0 }
        change_source: { enum: [user_selection, import, legacy_baseline] }
    LexicalEntryDetails:
      type: object
      required: [entry, history, occurrences]
      properties:
        entry: { $ref: "#/components/schemas/LexicalEntry" }
        history: { type: array, items: { $ref: "#/components/schemas/LexicalStatusHistory" } }
        occurrences: { type: array, items: { $ref: "#/components/schemas/LexicalOccurrence" } }
    NormalizeLexicalRequest:
      type: object
      required: [language, value]
      properties:
        language: { type: string }
        value: { type: string }
    CorrectLemmaRequest:
      type: object
      required: [language, original, corrected]
      properties:
        language: { type: string }
        original: { type: string }
        corrected: { type: string }
    LexicalNormalization:
      type: object
      required: [original, normalized, provider, version, user_corrected]
      properties:
        original: { type: string }
        normalized: { type: string }
        provider: { type: string }
        version: { type: string }
        user_corrected: { type: boolean }
    PhraseCandidate:
      type: object
      required: [canonical_form, display_form, normalized_form, token_start, token_end, reason]
      properties:
        canonical_form: { type: string }
        display_form: { type: string }
        normalized_form: { type: string }
        token_start: { type: integer, minimum: 0 }
        token_end: { type: integer, minimum: 0 }
        reason: { type: string }
    LearningResource:
      type: object
      required: [id, display_name, version, source_url, license, checksum_sha256, size_bytes, local_path, state, installed_bytes, error, updated_at_ms]
      properties:
        id: { type: string }
        display_name: { type: string }
        version: { type: string }
        source_url: { type: string }
        license: { type: string }
        checksum_sha256: { type: string }
        size_bytes: { type: integer, minimum: 0 }
        local_path: { type: [string, "null"] }
        state: { enum: [available, installing, installed, failed] }
        installed_bytes: { type: integer, minimum: 0 }
        error: { type: [string, "null"] }
        updated_at_ms: { type: integer, minimum: 0 }
    SubtitleSearchRequest:
      type: object
      required: [api_key, language]
      properties:
        provider: { type: string, default: opensubtitles }
        api_key: { type: string, writeOnly: true }
        language: { type: string }
        query: { type: [string, "null"] }
        moviehash: { type: [string, "null"] }
    SubtitleDownloadRequest:
      type: object
      required: [api_key, file_id]
      properties:
        provider: { type: string, default: opensubtitles }
        api_key: { type: string, writeOnly: true }
        file_id: { type: integer, minimum: 0 }
    SubtitleSearchResult:
      type: object
      required: [id, file_id, language, release, source, rating, download_count]
      properties:
        id: { type: string }
        file_id: { type: integer, minimum: 0 }
        language: { type: string }
        release: { type: string }
        source: { type: string }
        rating: { type: number }
        download_count: { type: integer, minimum: 0 }
    WordProfile:
      type: object
      required: [id, language, lemma, normalized_lemma, display_form, status, updated_at_ms, user_definition, personal_note, learning_updated_at_ms]
      properties:
        id: { type: string }
        language: { type: string }
        lemma: { type: string }
        normalized_lemma: { type: string }
        display_form: { type: string }
        status:
          anyOf:
            - enum: [unknown_meaning, known_not_recognized, known_recognized]
            - type: "null"
        updated_at_ms: { type: integer, minimum: 0 }
        user_definition: { type: [string, "null"] }
        personal_note: { type: [string, "null"] }
        learning_updated_at_ms: { type: integer, minimum: 0 }
    UpdateWordProfile:
      type: object
      required: [language, lemma, display_form]
      properties:
        language: { type: string }
        lemma: { type: string }
        display_form: { type: string }
        status:
          anyOf:
            - enum: [unknown_meaning, known_not_recognized, known_recognized]
            - type: "null"
        source:
          anyOf:
            - $ref: "#/components/schemas/SourceContext"
            - type: "null"
    BatchWordProfiles:
      type: object
      required: [language, lemmas]
      properties:
        language: { type: string }
        lemmas:
          type: array
          maxItems: 5000
          items: { type: string }
    CreateWordObservation:
      type: object
      required: [word_profile_id, sentence_id, original_form]
      properties:
        word_profile_id: { type: string }
        sentence_id: { type: string }
        original_form: { type: string }
        result:
          anyOf:
            - enum: [recognized_in_context, not_recognized_in_context]
            - type: "null"
        clear: { type: boolean, default: false }
        source:
          anyOf:
            - $ref: "#/components/schemas/SourceContext"
            - type: "null"
    SourceContext:
      type: object
      required: [language, normalized_lemma, original_form, sentence_text, media_title, media_fingerprint, start_ms, end_ms]
      properties:
        language: { type: string }
        normalized_lemma: { type: string }
        media_id: { type: [string, "null"] }
        sentence_id: { type: [string, "null"] }
        original_form: { type: string }
        sentence_text: { type: string }
        media_title: { type: string }
        media_fingerprint: { type: string }
        start_ms: { type: integer, minimum: 0 }
        end_ms: { type: integer, minimum: 0 }
    WordObservation:
      type: object
      required: [id, word_profile_id, sentence_id, original_form, result, created_at_ms]
      properties:
        id: { type: string }
        word_profile_id: { type: string }
        sentence_id: { type: string }
        original_form: { type: string }
        result: { enum: [recognized_in_context, not_recognized_in_context] }
        created_at_ms: { type: integer, minimum: 0 }
    WordOccurrence:
      type: object
      required: [id, source_key, word_profile_id, original_form, sentence_text_snapshot, media_title_snapshot, media_fingerprint_snapshot, start_ms_snapshot, end_ms_snapshot, first_seen_at_ms, last_seen_at_ms, encounter_count]
      properties:
        id: { type: string }
        source_key: { type: string }
        word_profile_id: { type: string }
        media_id: { type: [string, "null"] }
        sentence_id: { type: [string, "null"] }
        original_form: { type: string }
        sentence_text_snapshot: { type: string }
        media_title_snapshot: { type: string }
        media_fingerprint_snapshot: { type: string }
        start_ms_snapshot: { type: integer, minimum: 0 }
        end_ms_snapshot: { type: integer, minimum: 0 }
        first_seen_at_ms: { type: integer, minimum: 0 }
        last_seen_at_ms: { type: integer, minimum: 0 }
        encounter_count: { type: integer, minimum: 1 }
    WordStatusHistory:
      type: object
      required: [id, word_profile_id, previous_status, new_status, source_occurrence_id, changed_at_ms, change_source]
      properties:
        id: { type: string }
        word_profile_id: { type: string }
        previous_status: { type: [string, "null"] }
        new_status: { type: [string, "null"] }
        source_occurrence_id: { type: [string, "null"] }
        changed_at_ms: { type: integer, minimum: 0 }
        change_source: { enum: [user_selection, import, legacy_baseline] }
    WordDetails:
      type: object
      required: [profile, history, occurrences]
      properties:
        profile: { $ref: "#/components/schemas/WordProfile" }
        history: { type: array, items: { $ref: "#/components/schemas/WordStatusHistory" } }
        occurrences: { type: array, items: { $ref: "#/components/schemas/WordOccurrence" } }
    VocabularyAssetBundle:
      type: object
      required: [version, exported_at_ms, profiles, history, occurrences, observations, lexical_entries, lexical_history, lexical_occurrences, phonetic_finding_feedback]
      properties:
        version: { enum: [1, 2, 3, 4] }
        exported_at_ms: { type: integer, minimum: 0 }
        profiles: { type: array, items: { $ref: "#/components/schemas/WordProfile" } }
        history: { type: array, items: { $ref: "#/components/schemas/WordStatusHistory" } }
        occurrences: { type: array, items: { $ref: "#/components/schemas/WordOccurrence" } }
        observations: { type: array, items: { $ref: "#/components/schemas/WordObservation" } }
        lexical_entries: { type: array, items: { $ref: "#/components/schemas/LexicalEntry" } }
        lexical_history: { type: array, items: { $ref: "#/components/schemas/LexicalStatusHistory" } }
        lexical_occurrences: { type: array, items: { $ref: "#/components/schemas/LexicalOccurrence" } }
        phonetic_finding_feedback: { type: array, items: { type: object } }
    DictionaryLookup:
      type: object
      required: [query, lemma, definitions, phonetics, provider, cached_at_ms]
      properties:
        query: { type: string }
        lemma: { type: string }
        definitions:
          type: array
          items:
            type: object
            required: [part_of_speech, text]
            properties:
              part_of_speech: { type: [string, "null"] }
              text: { type: string }
        phonetics:
          type: array
          items:
            type: object
            required: [text, region, audio_url]
            properties:
              text: { type: string }
              region: { type: [string, "null"] }
              audio_url: { type: [string, "null"] }
        provider: { type: string }
        cached_at_ms: { type: integer, minimum: 0 }
    DictionaryLookupBundle:
      type: object
      required: [query, normalized_lemma, results]
      properties:
        query: { type: string }
        normalized_lemma: { type: string }
        results:
          type: array
          items:
            type: object
            required: [provider, lookup, error]
            properties:
              provider:
                type: object
                required: [id, display_name, supported_languages, provides_definitions, provides_phonetics, provides_audio, offline]
                properties:
                  id: { type: string }
                  display_name: { type: string }
                  supported_languages: { type: array, items: { type: string } }
                  provides_definitions: { type: boolean }
                  provides_phonetics: { type: boolean }
                  provides_audio: { type: boolean }
                  offline: { type: boolean }
              lookup:
                anyOf:
                  - $ref: "#/components/schemas/DictionaryLookup"
                  - type: "null"
              error: { type: [string, "null"] }
    ExternalVocabularyImport:
      type: object
      required: [language, entries, default_status, overwrite_existing]
      properties:
        language: { type: string }
        entries:
          type: array
          items:
            type: object
            required: [word, status]
            properties:
              word: { type: string }
              status:
                anyOf:
                  - enum: [unknown_meaning, known_not_recognized, known_recognized]
                  - type: "null"
        default_status:
          anyOf:
            - enum: [unknown_meaning, known_not_recognized, known_recognized]
            - type: "null"
        overwrite_existing: { type: boolean }
    ExternalVocabularyImportSummary:
      type: object
      required: [created, initialized, skipped, overwritten, invalid]
      properties:
        created: { type: integer, minimum: 0 }
        initialized: { type: integer, minimum: 0 }
        skipped: { type: integer, minimum: 0 }
        overwritten: { type: integer, minimum: 0 }
        invalid: { type: integer, minimum: 0 }
    SentenceDiagnosis:
      type: object
      required: [sentence_id, hints, unclassified_lemmas]
      properties:
        sentence_id: { type: string }
        hints:
          type: array
          items:
            type: object
            required: [kind, message, word_profile_ids]
            properties:
              kind:
                enum: [meaning_barrier, recognition_barrier, insufficient_information, other_factors]
              message: { type: string }
              word_profile_ids:
                type: array
                items: { type: string }
        unclassified_lemmas:
          type: array
          items: { type: string }
    Error:
      type: object
      required: [code, message, correlation_id, retryable]
      properties:
        code: { type: string }
        message: { type: string }
        correlation_id: { type: string }
        retryable: { type: boolean }
