# Phase 3.4.2 Plan: Semantic / Prosodic Group Separation

状态：IN_PROGRESS

启动时间：2026-07-07 CST

共享上下文：
`../3.0-english-listening-learning-loop/3.4.X-LEARNING-DOMAIN-V2-SHARED-CONTEXT.md`

架构决策：`docs/decisions/0016-sense-group-semantic-prosodic-separation.md`

## Objective

新增句子 token-span 型 SenseGroup 语义加工层，同时保留现有 ChunkTimeline 的音频时间和
声学/韵律证据。SenseGroup 是句子级文本标注，不是全局学习资产；播放范围通过 WordTimeline
投影获得。

## User Value

> "我可以按语义单位理解一句话，而不只是按说话者的停顿来切分；练习和回听都能以意义单位
> 为目标，即使没有高级语法分析器也能工作。"

## Work Plan

### Slice 0 — Planning and ADR

- [x] 撰写 ADR 0016（SenseGroup 身份、语义/韵律分离、对齐模型、provider 策略）。
- [x] 细化 3.4.2-PLAN.md 为可检查项。
- [ ] 更新 STATE.md。

### Slice 1 — Domain contract

- [ ] 新增 `crates/domain/src/sense_group.rs`。
- [ ] 新增 string_id: `SenseGroupAnalysisId`, `SenseGroupId`。
- [ ] `SenseGroupAnalysis` 结构：id, track_id, media_id, parent_word_timeline_id,
      provider_id/version/algorithm, status(TimelineStatus), created_by(TimelineCreator),
      metrics_json(TimelineMetrics), groups: Vec<SenseGroup>, timestamps。
- [ ] `SenseGroup` 结构：id, sentence_id, group_index, start_token_index,
      end_token_index(inclusive), text, label(Option), head_token_index(Option),
      confidence, sources: Vec<SenseGroupSource>。
- [ ] `SenseGroupSource` 枚举：DependencyParse, PhraseStructure, Punctuation, LengthLimit,
      Rule, User。
- [ ] `SenseGroupAnalysisSummary` 读模型。
- [ ] `LLTimelineDocument` 新增 `sense_group_analyses` 和 `active_sense_group_analysis_id`
      字段（serde default，向后兼容）。
- [ ] 播放投影纯函数：`sense_group_playback_range()`。
- [ ] 序列化/反序列化和投影函数的单元测试。
- [ ] 不改 ChunkTimeline、不改 schema、不改 Flutter。

### Slice 2 — Rule-based fallback provider

- [ ] 新增 `crates/speech-analysis/src/sense_group_partition.rs`。
- [ ] `SenseGroupPartitionConfig`：min_words, max_words, target_groups_per_sentence。
- [ ] 规则回退算法（不依赖外部模型）：
  - 强标点（句号/分号）强制分组边界。
  - 弱标点（逗号）加分。
  - 长度限制（软/硬 max words）。
  - 短语保护（复用 `PhraseCandidate`）。
  - 目标粒度 3-5 组/句。
- [ ] 英语 fixture 测试（≥5 句不同结构）。
- [ ] 中文 fixture 测试（≥3 句）。
- [ ] Provider 常量：PROVIDER_ID = "rule-based-sense-group", PROVIDER_VERSION = "v1"。

### Slice 3 — Persistence (schema v23)

- [ ] migration `0023_sense_group_analyses.sql`。
- [ ] `sense_group_analysis_runs` 表 + indexes + UNIQUE active-per-track 约束。
- [ ] SubtitleRepository trait 新增 7 方法。
- [ ] TimelineResourceRepository blanket impl 转发。
- [ ] SQLite 实现。
- [ ] 迁移测试、lifecycle 测试、round-trip 测试。

### Slice 4 — Application and API

- [ ] `crates/application/src/sense_groups.rs`：generate, list, get, activate, archive, delete。
- [ ] API 路由（`routes/timelines.rs`）：
  - `GET /timelines/{track_id}/sense-group-summaries`
  - `GET /timelines/sense-groups/{id}`
  - `POST /timelines/{track_id}/sense-group-generate`
  - `POST /timelines/sense-groups/{id}/activate`
  - `POST /timelines/sense-groups/{id}/archive`
  - `DELETE /timelines/sense-groups/{id}`
- [ ] LLTimeline import/export 集成。
- [ ] OpenAPI spec 更新。
- [ ] API 测试。

### Slice 5 — Flutter integration

- [ ] Dart 模型：SenseGroupAnalysis, SenseGroup, SenseGroupAnalysisSummary。
- [ ] ApiService 新增端点调用。
- [ ] SubtitleController：senseGroupsBySentence 缓存。
- [ ] TokenLine widget 支持 sense group rendering。
- [ ] PlaybackActionsCoordinator：seekSenseGroup, loopSenseGroup。
- [ ] 设置：showSenseGrouping toggle。
- [ ] Dart fixture contract test。

### Slice 6 — Alignment and closeout

- [ ] 评估 ChunkTimeline 重命名决策（记录证据和结论）。
- [ ] 如证据充分定义 alignment 数据结构；否则推迟。
- [ ] 更新 3.4.2-PLAN.md 标记 COMPLETED。
- [ ] STATE.md, CHANGELOG.md 更新。

## Exit Signals

1. 同一句可以同时拥有语义分组和实际韵律分组，且两者互不覆盖。
2. text-only SenseGroup 可通过词时间轴获得安全播放范围。
3. SenseGroup 默认是 annotation/practice target，不自动进入全局词汇资产。
4. 没有 parser/model 时现有 Chunk replay 不回退。
5. 规则回退 provider 对英语和中文 fixture 产出合理分组。
6. `cargo test --workspace` 和 `flutter test` 通过。

## Guardrails

- 不把所有自由组合意群建立长期 known/unknown 状态。
- 不用句法边界冒充真实音频边界。
- 不在 spike 前锁定 UDPipe 为正式依赖。
- 不在算法未验证前全局重命名 chunk 引用。
- 不混入 Construction、SentencePattern 或其他 3.4.3 工作。

## Verification Commands

迭代中按改动范围运行 focused tests；收口至少运行：

```bash
cargo test --workspace
cd apps/desktop && flutter test
git diff --check
```
