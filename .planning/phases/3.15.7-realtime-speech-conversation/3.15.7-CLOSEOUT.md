# Phase 3.15.7 Closeout: Realtime Speech Conversation

状态：CODE COMPLETE — OWNER REAL-PROVIDER QA PENDING

日期：2026-07-16

## Delivered

- provider-neutral realtime 深模块：profile/session/ordered turn/audio/provider transcript/local
  transcript 与 terminal lifecycle；content anchor 仅为可空 surface context。
- OpenAI Realtime 与 Qwen Omni Realtime 两个异构 WebSocket adapter，共用 neutral contract suite，
  并保留各自 session/event/transcript 语义和 honest error mapping。
- bearer-protected localhost WebSocket bridge；provider secret 只由 sidecar 从 macOS Keychain 解析，
  Flutter、SQLite、HTTP response 与普通日志均不持有 secret。
- macOS AVAudioEngine 全双工桥：24 kHz mono PCM16 实时上行、16 kHz mono PCM16 WAV 本地落盘、
  assistant PCM 流式播放、barge-in flush 与统一 audio-focus/cleanup。
- 首个内容锚定 Speaking surface：live provider caption、assistant transcript/audio、Finish/Cancel/
  disconnect 状态与明确 provider/local transcript 标签；当前布局没有进入共享领域 identity。
- local Whisper authority：只有 completed local learner transcript 才能 finalize turn 并写 spoken
  production-corpus projection；失败/中断零半截 corpus。projection 可重建，3.15.6 仍为只读。

## Boundaries Preserved

- provider transcript 只服务即时体验，不能成为 learner-output authority，也不能回填 local 字段。
- 零 observation/evidence/capability/proposal/review writer；3.17 confirmation gate 保留。
- 3.15.9 TTS 仍是 auxiliary rendering/cache，零 attempt/evidence/projection/corpus writer；本 phase
  未重做其 owner 听感 QA。
- 未修改已知 hunting 毫秒 event-id flaky。
- 开放 chat、role-play、个人表达等后续 surface 可复用事实/lifecycle，不被当前 media anchor 或
  Speaking UI 绑定。

## Verification

- 两 adapter neutral codec/contract、domain lifecycle、application/repository/API、migration、secret
  与 spoken authority 负测已覆盖。
- OpenAPI implementation drift、architecture guards 与 contract examples 通过。
- Flutter analyze 与 macOS Debug native build 通过。
- `./scripts/test.sh --quick --strict --low-memory --json`：fmt、Clippy strict、Rust lib 609 与
  Flutter analyze 通过。
- 最终 `./scripts/test.sh --full --strict --low-memory --json`：Rust workspace 667、Flutter 444、
  analyze 与 contracts 全绿；OpenAPI/architecture 与 5 个 contract examples 通过。
- macOS Debug native build 通过；真实 provider/device QA 仍按 `3.15.7-MANUAL-QA.md` pending。

## Deferred QA

当前工作区没有接收或存储 owner API key，因此没有把 fake server/codec fixture 冒充真实 provider
可达性。至少一家真实 provider 的 socket、麦克风、扬声器、barge-in 与 local Whisper 闭环需要
owner 在 App 内输入 key 后执行；通过前不标 Phase COMPLETE。
