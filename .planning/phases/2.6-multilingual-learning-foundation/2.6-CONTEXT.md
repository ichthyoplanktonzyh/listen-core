# Phase 2.6 Context: Multilingual Learning Foundation

更新时间：2026-06-20 23:40 CST

## Background

LLPlayerNext 当前主要服务英语听力学习。已经落地的播放器、字幕资源、时间轴资源、
词汇状态和诊断体系，为多语言学习提供了可复用底座，但很多学习层能力仍然是英语优先：

- `TXT-001` 明确是英语 token 化。
- `subtitle-core` 当前使用 `tokenize_english()`。
- Flutter client 多处请求仍硬编码 `language=en`。
- 词汇状态、字典、lemma、发音和规则型语流提示主要围绕英语实现。
- Chunk / PhoneTimeline 的研究语境当前主要是英语。

Phase 2.6 的目标不是立刻支持世界所有语言，而是建立可持续扩展的多语言学习基础。
首批验收语言明确为：

```text
English + Chinese
```

英语继续作为当前主线，汉语作为第一种真正暴露架构假设的非空格分词语言。

## Product Motivation

产品长期方向是支持主要语言的听力学习，而不是只做英语工具。这里的“多语言”不是
营销意义上的“所有语言”，而是：

- 重要语言优先。
- 每种语言有明确能力矩阵。
- 语言相关能力通过 provider / profile / capability 声明进入系统。
- 没有能力的语言干净降级，不假装完整支持。

汉语是最适合作为第二语言基线的原因：

- 它不依赖空格分词，能迫使 tokenizer 从英语假设中解耦。
- 它同时需要字、词、短语多粒度学习单位。
- 它有拼音、声调、变调、轻声、儿化等独特发音/声音模式。
- 外国人学汉语的核心困难和英语不同，能检验诊断模型是否足够抽象。

## Current Reusable Foundation

已经具备并可复用：

- 媒体播放、跳转、循环、播放位置监听。
- SRT / WebVTT / LLTimeline 字幕资源生命周期。
- SubtitleTrack / SubtitleSentence / SubtitleToken 基础模型。
- `LanguageCode` 与词汇状态语言隔离。
- 词汇状态、状态历史、来源快照、词汇本。
- 字典 provider 抽象。
- WordTimeline / ChunkTimeline / PhoneTimeline 的资源化方向。
- 本地 SQLite 持久化和导入导出基础。
- UI 本地化：界面语言已有 English / 简体中文。

## Current English Assumptions

需要解耦的英语假设：

1. **Tokenizer**
   - 当前 `tokenize_english()` 会把连续汉字当作一个 word token。
   - 汉语需要分词，并且需要保留字级 fallback。

2. **Lexical Unit**
   - 当前学习单位近似等于英语 lemma。
   - 汉语需要支持 char / word / phrase 多粒度学习单位。

3. **Language Hardcoding**
   - 桌面 API client、词汇本、字典查询、来源快照多处固定 `language=en`。
   - 需要从字幕轨、用户选择或媒体资源中传递学习语言。

4. **Dictionary**
   - 当前 provider 主要支持英语。
   - 汉语需要中英释义、拼音、声调、例句、HSK/频率等字段。

5. **Pronunciation**
   - 英语使用 CMUdict / ARPAbet / IPA / 连读弱读规则。
   - 汉语需要拼音、声母韵母、声调、变调和轻声。

6. **Diagnosis**
   - 当前诊断主要围绕“不认识词 / 认识但听不出词”。
   - 汉语还需要表达“认识汉字但不认识词”“声调听不出”“词边界切分失败”等问题。

## Scope

Phase 2.6 聚焦建立语言能力底座：

1. 定义 language learning profile / capability matrix。
2. 引入 language-aware tokenizer。
3. 把学习单位从 `lemma` 扩展为 `LexicalUnit`。
4. 去除桌面端和 API client 的 `language=en` 硬编码。
5. 接入汉语最小词典 / 拼音 provider。
6. 为汉语建立最小可用学习面板和诊断。
7. 建立英语 + 汉语双语言回归测试。

## Non-Goals

- 不在本阶段支持所有语言。
- 不引入完整多语言 ASR / FA / PhoneTimeline 生产能力。
- 不要求汉语一开始拥有英语同等的 ChunkTimeline / Sound Pattern 深度。
- 不重写现有英语词汇资产。
- 不破坏英语当前行为。
- 不把界面语言和学习语言混为一谈。

## Success Criteria

- 英语路径保持不回归。
- 汉语字幕可以被正确分词或以可解释 fallback 方式拆分。
- 用户可以在汉语 token / lexical unit 上建立词汇状态、来源快照和词汇本条目。
- 字典查询可按 `zh` provider 返回拼音和基础释义。
- API client 不再固定 `language=en`。
- 当前学习语言来自字幕轨或用户明确选择。
- 文档明确后续语言扩展方式和能力矩阵。

## Inputs

- `.planning/REQUIREMENTS.md`：TXT / WORD / DICT / PRON 需求。
- `.planning/PROJECT.md`：产品定位与学习诊断模型。
- `.planning/ROADMAP.md`：M2 资源主线。
- `crates/subtitle-core/src/lib.rs`
- `crates/domain/src/lib.rs`
- `crates/application/src/lexical.rs`
- `crates/application/src/subtitles.rs`
- `crates/application/src/vocabulary.rs`
- `crates/dictionary-provider/src/lib.rs`
- `apps/desktop/lib/services/api_service.dart`
- `apps/desktop/lib/main.dart`
