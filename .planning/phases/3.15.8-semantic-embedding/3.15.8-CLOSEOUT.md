# Phase 3.15.8 Semantic Embedding — Code Complete Closeout

状态：✅ CODE COMPLETE — OWNER MANUAL QA PENDING  
日期：2026-07-16  
起点：`main@13bd9bd1`  
分支：`codex/phase-3.15.8-semantic-embedding`

## Delivered

- 完成 main 实际底盘审计并将 PLAN 修订为 v2；真实 spike 覆盖 FastEmbed in-process 与 Ollama
  local HTTP 两条异构实现，并实现/测试 OpenAI-compatible seam。详见 `3.15.8-SEAM-SPIKE.md`。
- 新 `embedding-provider` deep module：显式 opt-in local model install/enable/disable/uninstall，完整
  descriptor/fingerprint，+0B model base；兼容 adapter 不默认进入 production composition。
- SQLite schema v42 disposable vector read model：float32 LE codec、source text hash、exact model
  fingerprint、单事务全量 replace；source/model 变化 stale，不做 cross-space migration。
- 统一 semantic search 覆盖 media phrase/chunk 与 production document/lexeme，带 source/model
  provenance、language/source/channel filters 与 stable cosine top-K。
- 3.15.6 gap v1 保持原 top-K/readiness/ranking；新 endpoint/UI 只对既有 target 追加 threshold +
  model fingerprint 的 near-semantic clue，零 candidate/writer。
- OpenAPI、typed Dart models/client 与词汇册最小 surface 贯通安装、重建、搜索、停用/启用/删除；
  surface 不进入 domain identity。

## Boundary Proof

- embedding source adapter 只读 media corpus 与 3.15.5/3.15.7 production projection；没有读取
  provider caption 作为 learner output。3.15.7 local-authoritative source rule 未改。
- semantic application/provider/runtime 均没有 attempt、observation、evidence、capability/proposal、
  review 或 TTS repository；持久化测试对相关 writer count 做前后不变断言。
- uninstall 只删除模型目录与 v42 derived index；3.17 confirmation gate、3.15.6 v1 与 exact FTS
  保留。

## Verification

- 真实 FastEmbed production adapter：revision/artifact/dimension/fingerprint + 2-vector smoke PASS。
- 真实 Ollama `/api/embed` 384-d heterogeneous smoke PASS；两实现数值空间差异已记录。
- `cargo test --workspace --quiet`：674 tests PASS；随后补入 provider failed-state 契约测试，
  `embedding-provider` focused 2/2 PASS（当前 suite 共 675 tests）。
- `cargo clippy --workspace --all-targets -- -D warnings` PASS。
- `./scripts/validate-contracts.sh` PASS。
- Flutter full 446 PASS；semantic transport focused 15/15 PASS；`flutter analyze` PASS。
- `flutter build macos --debug` PASS；仅第三方 video_player deprecation/build-phase warnings。
- `git diff --check` PASS。

## Remaining Owner Boundary

执行 `3.15.8-MANUAL-QA.md`：首次真实安装、离线重启、大语料 latency/RSS、stale/rebuild、结果质量
与 gap explanation 体验。未完成前不宣称 Phase COMPLETE/owner accepted。

本 phase 未执行 3.15.7 真实 provider/device QA，未重做 3.15.9 owner 听感 QA，也未修改 hunting
毫秒 event-id flaky。
