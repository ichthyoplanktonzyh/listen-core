# Phase 3.18 Closeout: Cross-modal Coach

状态：CODE COMPLETE — OWNER MANUAL QA PENDING

日期：2026-07-18

## Delivered

- 3.10 listening-only envelope 扩为四通道只读 read model。attempt、supporting judgment、
  adjudication、observation、proposal、confirmed projection、capability history 与
  personal-expression attempt 保持独立 authority layer；effective assessment 继续读取 3.17
  projection/override authority，通道之间无推断。
- SQLite 聚合保持 bounded live query，没有 schema migration、durable Coach cache 或 rebuild writer。
  `generated_at_ms` 表达 freshness；返回 Coach 时显式刷新。10k learning-event 性能基线继续通过。
- evidence drill-down 增加 typed source kind、immutable snapshot、source availability 与
  `source_media_unavailable` 降级。来源丢失不隐藏历史，也不产生失败事实。
- suggestion action 字符串替换为 typed destination：Review Queue、Hunting List、Cross-modal
  Review、Personal Expression 与 Content Home。Flutter 保存 Coach 的 language/days/suggestion/
  scroll return context，真实 consumer 返回后刷新并恢复滚动；Cross-modal Review 可直接打开。
- Dashboard 呈现四通道 assessment/非零事实、诚实 starter state 与 provider-independent LLM
  availability。没有综合等级；`UserSentencePattern` 只作为 durable asset invitation。

## Authority Audit

- Coach 只持有 `CoachDashboardRepository` read port；没有注入 mixed capability/semantic/review/
  personal-expression writer port，也未调用 3.17 的 POST audit/rebuild。
- 自动测试对 aggregation 前后 observation/proposal/decision/history 行数做负向断言。
- 3.17 confirmation transaction、override priority、writing unassessed 与 immutable history 未改。
- Hunting List 未迁移；FocusTarget 未重开；embedding/corpus 未成为 evidence/capability truth。

## Verification

- `cargo test -p application -p persistence-sqlite -p api-http`：65 + 133 + 52 unit tests、
  23 integration/migration tests 与 doc tests 全绿。
- `cargo clippy -p application -p persistence-sqlite -p api-http --all-targets -- -D warnings`：通过。
- `flutter analyze`：通过。
- `flutter test`：453 passed。
- `./scripts/validate-contracts.sh`：architecture guards、OpenAPI 与全部 contract fixtures 通过。
- `git diff --check`：通过。

## Pending Owner Boundary

实现者没有执行 owner manual QA。下一步仅由 owner 按 `3.18-MANUAL-QA.md` 验证真实媒体、
真实 owner 数据、来源失联、typed return 与 GUI 体验。Phase 3.17、3.16、3.15.7、3.15.8、
3.15.9、3.14 的 pending owner QA 仍各自独立，本阶段未执行、冒充或夹带修复。
