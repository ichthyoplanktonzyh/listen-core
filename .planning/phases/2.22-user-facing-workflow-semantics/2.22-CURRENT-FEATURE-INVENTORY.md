# Phase 2.22 Current Feature Inventory

状态：COVERAGE CHECKLIST + VERIFIED P0 TEMPLATES

日期：2026-07-01

## Purpose And Boundary

This document is a **coverage checklist**, not the semantic model. It answers
"which features must Phase 2.22 account for?" — nothing more.

The authoritative semantic models are:

- `2.22-FEATURE-SEMANTICS-MODEL.md` — capability layers, readiness enum, names.
- `2.22-USER-VISIBLE-STATE-MACHINE.md` — surface regions and readiness lanes.

Earlier this file only listed bare feature names and reused the module split from
`worktree-ui-feature-semantic-mapping/ui-analysis/06-feature-interaction-map.md`.
A bare name list is a checklist, not a model. This revision separates the two:

- Section "Verified P0 Templates" fills the audit template for the core
  learning-path features, each verified against current `apps/desktop` code.
- Section "Coverage Checklist (not yet templated)" keeps every remaining feature
  as a tracked item, explicitly marked as pending verification. It must not be
  read as a completed model.

Rule: do not fabricate template fields for features not yet read in code. Move an
item up into "Verified P0 Templates" only after confirming its entry, states and
surfaces against the code.

## Audit Template

Every feature should eventually be documented with:

```text
Feature:
User intent:
Canonical entry:
Secondary entries:
Preconditions:
Readiness states:
Primary surface:
Visual feedback:
Fallback/degraded behavior:
Related internal resources:
Test path:
```

## Verified P0 Templates

Verified against current `main` (`apps/desktop`) on 2026-07-01. Each references
its state-machine region/lane in `2.22-USER-VISIBLE-STATE-MACHINE.md`.

### F1 Open local media

```text
Feature: Open a local video/audio file.
User intent: Play my own file for learning.
Canonical entry: AppBar "Open Media"; center CTA "Open Video/Audio" when empty.
Secondary entries: drag/drop media file; completed-download "Open".
Preconditions: core connected (R0.Connected).
Readiness states: R2 Empty -> Loading -> Playing.Local | -> Error.
Primary surface: R2 player surface + R6 bottom controls.
Visual feedback: R7 "Opening <file>" then video frame + active controls.
Fallback/degraded behavior: open failure -> R2.Error + R7 error string.
Related internal resources: media record, player adapter, saved progress.
Test path: manual smoke A (2.22-FRONTEND-E2E-QA.md).
```

### F2 Generate subtitles with local Whisper

```text
Feature: Generate subtitles from current media via local Whisper.
User intent: No subtitle file; produce one and learn immediately.
Canonical entry: AppBar primary/secondary subtitle menu "Generate".
Secondary entries: Transcription Center (recovery/fallback load).
Preconditions: media loaded; ASR model configured.
Readiness states: lane Word Sync unavailable -> generating -> available|degraded
  (C.1); Chunk Replay / Listening Structure lanes follow (C.2/C.3).
Primary surface: R6 typed subtitle-generation task chip; R8 generation dialog.
Visual feedback: task chip working/success; generated track loads into overlay.
Fallback/degraded behavior: no word timings -> Word Sync unavailable; missing
  rhythm_frames -> Listening Structure unavailable, explained honestly.
Related internal resources: transcription job, WordTimeline, document rhythm_frames.
Test path: manual smoke B; test/task_status_test.dart; test/backend_event_coordinator_test.dart.
```

### F3 Import SRT/VTT subtitle

```text
Feature: Attach an existing SRT/VTT as primary or secondary subtitle.
User intent: I already have a subtitle file.
Canonical entry: AppBar primary/secondary subtitle menu "Import".
Secondary entries: drag/drop subtitle file.
Preconditions: media loaded (subtitle-first drop warns "open media first").
Readiness states: Subtitles available; Word Sync / Chunk Replay / Listening
  Structure / Phone Evidence unavailable unless later generated/imported.
Primary surface: R3 overlay + R4 Transcript tab.
Visual feedback: overlay text; transcript follows current sentence.
Fallback/degraded behavior: capability strip (R4 Resources) shows the four
  learning lanes as unavailable rather than pretending readiness.
Related internal resources: SubtitleTrack.
Test path: manual smoke C; test/capability_readiness_test.dart (plain SRT case).
```

### F4 Word sync (word-level highlight)

```text
Feature: Highlight the current word in sync with audio.
User intent: See exactly which word I am hearing.
Canonical entry: automatic during playback when Word Sync lane is available.
Secondary entries: transcript word styling.
Preconditions: active WordTimeline OR generated word timings for the track.
Readiness states: lane C.1 (unavailable / generating / available[audio-backed] /
  degraded[estimated] / stale / error).
Primary surface: R3 TokenLine word highlight; also R4 Transcript.
Visual feedback: current word styled per wordHighlightStyle.
Fallback/degraded behavior: estimated timing -> degraded; must be labeled, not
  shown as audio-backed. (⚠️ SM-07: overlay re-derives instead of reading lane.)
Related internal resources: WordTimeline, timing sources, legacy word timings.
Test path: test/capability_readiness_test.dart; manual smoke B/D.
```

### F5 Chunk replay

```text
Feature: Replay meaningful phrase chunks, loop/expand them.
User intent: Re-listen to a phrase until I get it.
Canonical entry: bottom controls prev/next chunk, loop chunk, expand chunk.
Secondary entries: TokenLine chunk grouping click-to-seek.
Preconditions: Word Sync available; active ChunkTimeline or chunk partitions.
Readiness states: lane C.2 (unavailable[no word]/available/error).
Primary surface: R6 chunk controls + R3 TokenLine grouping.
Visual feedback: current chunk highlight; loop active; source-loop-stop visible.
Fallback/degraded behavior: no active chunk -> controls disabled with reason
  (SM-F4). (⚠️ SM-07: controls do not yet read the lane.)
Related internal resources: ChunkTimeline, chunk partitions.
Test path: manual smoke D.
```

### F6 Listening structure

```text
Feature: Show stress anchors, weak groups, compression, phrase boundaries, hotspots.
User intent: Know what to listen for in this sentence.
Canonical entry: R3 overlay listening layer, mode "Listening structure".
Secondary entries: diagnosis card evidence.
Preconditions: Word Sync available; document-level rhythm_frames present.
Readiness states: lane C.3 (unavailable / available[audio-supported] /
  degraded[predicted-only] / error).
Primary surface: R3 ListeningLayer.Mode.Structure (ExpectedPronunciationReference
  + RhythmFrameRibbon).
Visual feedback: anchors/weak/compression/boundaries/hotspots; hotspot click loops.
Fallback/degraded behavior: predicted-only -> degraded, never shown as measured
  audio; no frames -> unavailable ribbon, layer not collapsed (SM-F6).
Related internal resources: LLTimelineDocument.rhythmFrames, RhythmFrame, sound_analysis fallback.
Test path: test/capability_readiness_test.dart (predicted/degraded); manual smoke E.
```

### F7 Phone evidence

```text
Feature: Inspect phone-level detected/inferred evidence.
User intent: Detail for a specific hard spot.
Canonical entry: R3 overlay listening layer, mode "Phone evidence".
Secondary entries: diagnosis card; Phonetic/Audio Analysis Center.
Preconditions: active PhoneTimeline or sound analysis with detected phones.
Readiness states: lane C.4 (unavailable / [generating unwired SM-08] / available / error).
Primary surface: R3 ListeningLayer.Mode.Phones; R8 analysis center.
Visual feedback: phone evidence ribbon; no-phones -> unavailable ribbon (SM-F6).
Fallback/degraded behavior: absence is visible, not a silent collapse.
Related internal resources: PhoneTimeline, SoundAnalysis phone findings.
Test path: manual smoke E/H.
```

### F8 Online download

```text
Feature: Download an online URL via yt-dlp for local use.
User intent: Save a video to watch/learn offline.
Canonical entry: AppBar "Open URL" -> "Download Video".
Secondary entries: —.
Preconditions: yt-dlp path configured; valid URL.
Readiness states: R5 Hidden -> Downloading -> Completed | Failed -> Hidden.
Primary surface: R5 download bar (typed DownloadStatusSnapshot).
Visual feedback: progress; Completed shows Open; Failed shows error.
Fallback/degraded behavior: cancel/dismiss invalidates generation (SM-F7).
  (⛔ SM-03 split source state; ⛔ SM-04 no auto-dismiss.)
Related internal resources: OnlineMediaDownload, downloadedMediaPath.
Test path: manual smoke F.
```

## Coverage Checklist (not yet templated)

The lists below are a tracking checklist only. They are NOT a semantic model.
Items graduate into "Verified P0 Templates" (or into feature slices during the
frontend refactor) once verified against code. Do not treat a bullet here as a
completed or modeled feature.

#### Module 1: Media Import And Playback

- Open local media file.
- Open online media URL.
- Drag/drop media and subtitle files.
- Download online media through yt-dlp.
- Playback controls: play/pause, stop, seek, previous/next sentence, speed, volume.
- Audio track selection.
- Playback error and unsupported-media feedback.
- Media archive and missing-media recovery states.

### Module 2: Subtitle Acquisition And Track Management

- Import SRT/VTT as primary or secondary subtitle.
- Generate subtitles with local Whisper.
- Open Transcription Center and manage transcription jobs.
- Search/download OpenSubtitles.
- Extract embedded text subtitles with ffmpeg/ffprobe.
- Select embedded subtitle tracks when available.
- Load generated subtitle track after transcription completion.
- Primary/secondary visibility, offset, no-secondary-track state.

### Module 3: Subtitle Resource And Timeline Resource Lifecycle

- View subtitle resources for current media.
- Activate a subtitle resource.
- Archive, restore and delete subtitle resources.
- Export subtitle resource as SRT or `.lltimeline.json`.
- Import / attach LLTimeline resource.
- View WordTimeline / ChunkTimeline / PhoneTimeline summaries.
- Activate/archive/delete timeline candidates.
- Manual WordTimeline review and user-adjusted timeline activation.
- Production artifact and readiness visibility.

### Module 4: Subtitle Display And Transcript Interaction

- Dual subtitle overlay.
- Subtitle presets, fonts, colors, background opacity and position.
- Subtitle visibility and status-style visibility toggles.
- Primary/secondary subtitle offsets.
- Transcript side panel, current sentence tracking and auto-scroll.
- TokenLine word interaction.
- Word status styling in overlay and transcript.
- Keyboard shortcuts for playback and quick status marking.

### Module 5: Word Sync And Chunk Replay

- Word-level highlight from active WordTimeline or legacy word timings.
- Current-word tracking during playback, seek and speed changes.
- Chunk grouping display in TokenLine.
- Current chunk highlight.
- Previous/next chunk navigation.
- Loop chunk and source loop stop.
- Expand chunk / chunk playback affordances.
- ChunkTimeline diagnostics and degraded states.

### Module 6: Listening Structure And Phone Evidence

- Listening structure layer from document-level `rhythm_frames`.
- Expected pronunciation reference.
- Stress anchors, nuclei, weak groups, compression spans, phrase boundaries and listening hotspots.
- Rhythm cue loop / evidence replay.
- Phone evidence mode from PhoneTimeline / sound analysis.
- Phoneme ribbon on the text line.
- Phone evidence / audio analysis center.
- Analyze current sentence or whole subtitle track.
- Feedback on audio findings.
- Missing evidence states: no WordTimeline, no `rhythm_frames`, no phone evidence, no energy evidence.

### Module 7: Vocabulary And Learning Assets

- Word learning panel from subtitle token click.
- Lexical entry details: definitions, pronunciation, user note, custom definition.
- LearningStatus changes and immediate UI refresh.
- Vocabulary screen with search/filter/status views.
- Vocabulary import/export.
- External word-list import.
- Phrase candidates and phrase status.
- Learning assets screen.
- Learning resources screen.
- Correct lemma / lexical identity correction.
- Source sentence replay and missing-source states.

### Module 8: Diagnosis And Practice Readiness

- Current sentence diagnosis card.
- Lexical barrier diagnosis.
- Listening / sound evidence diagnosis.
- Language-aware diagnosis reasons.
- Practice/review backend readiness where available.
- Cloze / dictation / review backend foundations that need user-facing surfacing.
- Diagnosis refresh after status changes.
- Degraded diagnosis when dictionary, timing or sound evidence is missing.

### Module 9: Settings, Tools And System Feedback

- Settings dialog.
- FFmpeg / ffprobe / yt-dlp tool paths.
- Transcription defaults: quality, language, destination.
- Display preferences: subtitles, transcript, word/chunk styles.
- Audio/listening preferences: phoneme ribbon, listening structure, phone evidence mode.
- Pronunciation/audio analysis provider preferences.
- Learning language vs UI language.
- Logs export.
- Download status bar.
- Global status text/task feedback.
- Backend connection/start/error/retry state.

### Module 10: Planned Or Backend-Ready Surfaces

These are not fully user-facing yet, but Phase 2.22 should decide whether to expose, hide, or mark as backend-ready:

- Practice attempts.
- Review queue.
- Learning events.
- Corpus search / local YouGlish-like search.
- Difficulty and learner profile signals.
- Recording/shadowing foundations.
- Dashboard aggregation.

## P0 Audit Order

1. Media open/playback + no-media state.
2. Subtitle acquisition paths: SRT/VTT, local Whisper, embedded subtitle, OpenSubtitles.
3. Subtitle resources and active track capability summary.
4. Word sync / chunk replay readiness.
5. Listening structure / phone evidence semantic split.
6. Word learning panel and diagnosis update path.
7. Download/transcription/audio-analysis task feedback.
8. Settings labels that expose stale or internal semantics.

## Step 0 Audit Snapshot — 2026-07-01 13:22 CST

Verified against current `main` Flutter code, not the older UI worktree.

### Current Entrypoints

- Media open/playback:
  `apps/desktop/lib/main.dart` exposes local file open, URL resolve/play,
  yt-dlp download, drag/drop, playback controls, audio-track selection, embedded
  native subtitle selection, no-media center CTA, and core-starting/error/retry.
- Subtitle acquisition:
  AppBar primary/secondary subtitle menus cover SRT/VTT import, local Whisper
  generation, and OpenSubtitles search; More menu covers embedded text subtitle
  extraction and Transcription Center.
- Resource lifecycle:
  side panel Resources tab and full Subtitle Resources screen expose subtitle
  resource activation/archive/restore/delete/export plus `.lltimeline.json`
  import/export and timeline candidate actions.
- Learning overlay:
  `TokenLine` handles word click, phrase candidates, current word highlight,
  chunk grouping/highlight, chunk seek, and word status styling.
- Listening/phone layers:
  current overlay already prioritizes document-level `LLTimelineDocument.rhythmFrames`
  for Listening structure and falls back to `soundAnalysis.rhythmFrame`; Phone
  evidence still depends on active PhoneTimeline / sound analysis.
- Vocabulary/diagnosis:
  subtitle word click opens Word learning; Vocabulary screen and import/export
  exist; DiagnosisCard shows lexical/pronunciation/rhythm/phone evidence with
  analyze-current/whole-track actions.
- System/task feedback:
  backend SSE drives transcription/audio-analysis status strings; download bar
  is still driven by split `activeDownload` plus `downloadedMediaPath`; global
  status remains a free string.

### P0 Findings

- Resource panels showed raw `WordTimeline` / `ChunkTimeline` / `PhoneTimeline`
  availability but did not answer the user question: "which learning abilities
  are available now?"
- Listening structure and Phone evidence were partly correct in data flow, but
  UI labels still used stale `sound pattern` / `Listening rhythm` language and
  tooltips still implied `sound_analysis` was required for the whole layer.
- Plain SRT/VTT and missing-resource paths lacked a typed degraded/unavailable
  explanation for Word sync, Chunk replay, Listening structure, and Phone
  evidence.
- Local Whisper default path can load the generated track and refresh speech
  enhancements, but completion feedback still does not summarize generated
  subtitle + Word sync + Listening structure readiness.
- Side panel visibility is still tied to `subtitleController.visible ||
  selectedLexicalDetails != null || sidePanel == 1`, so Resources is independent
  but Transcript remains surprisingly coupled to subtitle visibility.
- Download completion/dismissal remains a P0 status debt: `activeDownload` and
  `downloadedMediaPath` can disagree, and the bar has no typed success/error
  lifecycle.

### First P0 Slice Completed

- Added frontend `CapabilityReadinessSnapshot` for Subtitles, Word sync, Chunk
  replay, Listening structure, and Phone evidence.
- Timeline resource summary now displays a compact user-facing "Learning
  capabilities" readiness strip before advanced resource details.
- Readiness distinguishes active subtitles, audio-backed word sync, estimated
  word timing, document-level listening structure, predicted-only listening
  structure, active chunk replay, and active phone evidence.
- Primary UI copy now uses `Listening structure` and `Phone evidence`; internal
  setting keys and advanced resource names remain stable.

### Next P0 Owners

- Step 2 owns local Whisper completion feedback: after generation, summarize
  generated track, Word sync, Listening structure, and Phone evidence readiness.
- Step 5 owns typed task/download status: replace split download state and
  free-string task categories.
- Step 6 owns side-panel/no-media/secondary-subtitle visibility semantics.
