# Phase 3.15 Plan: Writing Studio v1 — Reconstruction & Revision

状态：CODE COMPLETE — OWNER MANUAL QA PENDING（v4）

日期：2026-07-16（v4 code-complete 修订）

## v4 保鲜与实现裁决（2026-07-16）

- Phase 3.14 已 CODE COMPLETE；owner GUI/真人麦克风 QA 明确延期，不在 3.15 重开，也不构成
  Writing 的真实依赖。3.15 直接消费已落地的内容通道、任务场景底盘、来源恢复与 3.11
  append-only 证据契约。
- Reading/Speaking lifecycle seam 审计没有证明抽取通用 interface 的 leverage。Writing 因而拥有
  自己的 editor、草稿/修订状态机和 feedback disposition，不复制 Reading/Speaking controller，
  也不为统一外形新建 generic task controller。
- 3.11 migration 已把“Studio 首个 in-progress draft consumer”明确留给后续 additive 表；Writing
  以独立 scratch projection + append-only submitted revision/finding/disposition 事实实现，不放宽
  semantic rubric/attempt/judgment/adjudication 的 UPDATE/DELETE guard。
- Harper `harper-core` 的公开 Rust API 已复核；仓库固定可在当前工具链编译且不引入新 ML
  runtime 树的 `0.40.0`：本地 `Lint` 提供 Unicode-scalar span、
  `LintKind`、suggestions、message 与 priority，适合底层 grammar/spelling finding adapter；其
  `spanless_hash` 官方明示不跨版本稳定，不能作为 durable finding identity。首刀先固定项目自有
  finding contract/identity，再在 Slice 3 接 adapter，避免外部 crate shape 泄漏到领域层。

上游输入：Phase 3.11/3.12/3.13/3.13.5/3.14、
`.planning/phases/3.0-english-listening-learning-loop/3.11-3.18-FOUR-CHANNEL-SHARED-CONTEXT.md`、
LOOP-012/014/015。

外部参考输入（2026-07-15 补）：见
`.planning/discuss/studio-3.13-3.16-external-reference-library.zh.md` §4。
开工修订时建立 `3.15-REFERENCE-MATRIX.md`（模板见该文档 §5）。要点：
Harper 为首选本地 grammar finding spike（Rust/Apache 2.0，可在 3.14 期间
提前排，结论影响 WritingFeedbackProvider 本地形态；只承担分层 feedback
最底层，不上移到内容/组织判断）；ERRANT 只作 finding schema 与离线评测
参考，不集成 Python runtime；H5P Essay 定位为"目标表达字面命中"客观展示
的交互参考，**不作为无 LLM 默认路径**——该路径维持 rubric 对照 + 用户
自评；H5P Dictation 只参考 assistance snapshot 持久化。

## Phase 3.13.5 基线

- Writing 是当前内容的通道，不建独立 Studio 首页；只有来源文本/片段满足任务 capability
  时点亮。个人模板等资产驱动写作由资产层进入同一任务生命周期。
- 复用 3.13.5/3.14 已验证的内容通道、来源恢复与音频焦点行为，不复制 Reading/Speaking
  controller。3.14 未抽出通用 lifecycle interface；Writing implementation 自己拥有 editor、
  scratch projection、append-only revision、finding disposition 与 revision diff。
- 写作阶段编辑器占主导（宽屏目标 ≥60%）；来源为可折叠侧条，feedback 只在用户提交初稿并
  请求后出现。固定三栏、常驻反馈栏、bottom sheet 和多阶段 dialog 均不允许。
- 阶段为“读/听来源 → 初稿 → 请求分层反馈 → 修订 → 保存版本·可延迟复写”；未来阶段不可点。

## Objective

让 writing 从逐字听写进入意义生产：用 dictogloss 重构、片段摘要与观点回应形成初稿、
分层反馈、用户修订和跨时间复写的完整事实链。

## User Value

> “我不是把字幕抄对，而是听完后用自己的英语重构。系统先告诉我漏了哪层意思，再提示
> 两个可改进点；第二稿仍是我自己写的，几天后还能看到哪些问题反复出现。”

## Scope

### In

- Dictogloss：听完整片段、可记关键词、停止音频后重构；保存听取次数/assistance。
- 一句话摘要、50–100 词摘要、观点回应三类最小 writing task。
- 固定 SemanticRubric：内容完成优先于语言表面错误；多个版本共享同一 rubric。
- 分层 feedback：任务完成/信息遗漏 → 组织衔接 → 语境用词 → 语法拼写；每次建议数量有上限。
- Revision history：原稿、模型原始 feedback、用户采纳/拒绝、修订稿、diff 与再次出现事实。
- 用户文本永远是产出权威；模型可建议，不能自动替换后保存为用户稿。
- 显式目标词/phrase/construction 的新语境主动使用只保留 3.17 handoff 契约；v1 不提供
  observation writer，避免在没有资产目标确认入口时制造 writing 能力证据；不自动 projection。
- 无 LLM 时支持原文/rubric 对照、自评和版本保存。
- 内容旅程和资产旅程共享草稿/退出恢复；返回内容会话时保持媒体/阅读位置且不自动播放。

### Out

- 通用富文本编辑器、协同写作、云文档、长篇作文课程。
- 模型整篇代写、自动接受修改、单一 grammar score。
- 将逐字 dictation 成功映射为完整 writing acquired。
- writing capability projection（3.17）。

## Dependencies

- 3.11 Rubric/Judgment/Adjudication 与非 lexical target seam。
- 3.12 WritingFeedbackProvider 与结构化输出/降级；写作反馈的显示资格以 3.12.1
  对应用途的裁决为准，未获资格时以原文/rubric 对照 + 自评交付。
- 3.13 阅读姿态提供原文对照；3.14 目标表达与转写经验可复用。
- 3.13.5 提供内容通道、表面规则和场景恢复；3.14 closeout 明确 Reading/Speaking 没有足够
  同语义重复可抽 lifecycle interface。本 phase 保持各自状态机，只复用已存在的宿主能力。

## Key Work

1. **Slice 0 — 计划保鲜与 Harper 裁决**：✅ 吸收 3.14 closeout；复核 Harper 公开 API 与
   identity/version 风险，明确其只处理语法/拼写底层、不越权判断内容与组织；建立 Reference
   Matrix。真实 dependency adapter 随 Slice 3 接入项目自有 finding contract。
2. **Slice 1 — 事实与用例**：✅ backend fact contract：Dictogloss/Summary/Opinion Response、
   assistance/prompt snapshot validator、固定 rubric 下 typed 多稿、项目自有 finding/provenance、
   accept/reject disposition、schema v38 append-only persistence 与 HTTP/OpenAPI。Editor scratch
   projection 和 UI revision timeline 已随 Slice 2 落地。
3. **Slice 2 — 编辑器优先任务住户**：✅ 以 Writing 自有 controller 接入既有内容通道宿主；实现
   来源折叠、编辑器 ≥60%、自动保存、退出恢复和不可跳未来阶段；feedback 未请求时完全不占位。
4. **Slice 3 — 分层反馈与用户决定**：✅ finding category、severity（非总分）、source span、
   suggestion、provider provenance、user disposition、采纳/拒绝与 diff；内容层先于表面层。
5. **Slice 4 — 证据与 handoff**：✅ v1 保持零 writing observation/projection writer；submitted
   typed attempts、manual self-assessment、finding/disposition 分层持久。模型 suggestion 不算成功；
   反复问题/明确资产目标只 handoff 给 3.17。
6. **Slice 5 — 真实材料 QA 与 closeout**：✅ 自动化与 code closeout 完成；dictogloss + 观点回应、
   无模型、拒绝、恢复草稿、来源缺失、窄窗口与音频返回位置进入 owner 最小手工清单
   `3.15-MANUAL-QA.md`，不得在本任务操控 owner GUI。

## Exit Signal

- 用户完成“听/读片段 → 初稿 → 分层反馈 → 自主修订 → 保存版本”。
- 原稿永远可见；拒绝建议不会制造负面能力证据。
- 模型直接生成的文字无法进入 writing observation writer。
- 同一 rubric 的延迟复写可与首稿比较；换 rubric 时明确不可直接比较。
- 编辑、反馈和修订阶段视觉重心随阶段变化；写作时没有常驻反馈栏挤压编辑器。

## Guardrails

- 意义与任务完成优先，不以错误计数替代写作能力。
- 自动 feedback 是可纠正 finding，不是教师真理。
- 不把所有语法问题上升为 durable 学习资产；先记录 attempt，反复且用户确认后再进入复习。
- 用户文本与版本历史是 Writing 模块的权威；共享生命周期 interface 不暴露或接管编辑器内部状态。
