# Phase 3.12 Plan: Vendor-neutral LLM Provider

状态：IN PROGRESS（v3，2026-07-12 开工前按 3.11 落地契约 + 远端案例对照修订）

日期：2026-07-11（v3 修订 2026-07-12）

上游输入：Phase 3.11（已落地契约见 `crates/domain/src/semantic_task.rs`、
`crates/application/src/semantic_task.rs`）、
`.planning/phases/3.0-english-listening-learning-loop/3.11-3.18-FOUR-CHANNEL-SHARED-CONTEXT.md`、
`.planning/discuss/four-channel-product-and-vendor-neutral-llm-final.zh.md`。

修订记录：

- v3（2026-07-12，开工修订）：按 3.11 落地契约与 GitHub 远端案例对照，钉死以下裁决：
  1. **draft-not-domain-type 边界（正确性）**：provider trait 只返回内容草稿
     （`RubricDraft` / `JudgmentDraft`），绝不返回带 id/version 的 3.11 domain 类型；
     身份 fingerprint、版本续接、快照 hash、全部 validator 仍由 3.11 use case 服务端持有。
  2. **两层 trait**：`LlmChatAdapter`（wire seam，2 个异构协议实现它）+
     `SemanticRubricProvider`/`SemanticJudgeProvider`（application seam，`AppServices` 持有），
     由泛型 `LlmSemanticProvider<A: LlmChatAdapter>` 组合 prompt/schema/parse 一次写成——
     换 adapter 语义不变即中立性证明。
  3. **WritingFeedbackProvider 降范围**：dictogloss/summary 经 `SemanticJudgeProvider` 判
     （已是 `SemanticTaskKind` 变体），不新建独立 trait，等 3.15 揭示真实形状再定（§3.6 seam 纪律）。
  4. **build-vs-borrow = 手写**：用 `reqwest`（已是 workspace 依赖）手写 2 个 adapter；
     rust-genai 的中立-类型设计仅作只读蓝本，不引其 crate（避免其 `ChatRequest`/env-var auth 上浮）。
  5. **能力描述符**：借 LiteLLM 能力分类学（structured_output/streaming/multilingual/audio/
     context/provenance），但对自定义 endpoint/Ollama **必须 probe**（`CapabilityClaim::Probed`），
     不照抄"只声明"——远端无一真探测，此项刻意严于所有参考。
  6. **密钥 = Rust OS keychain + auth_ref**：security-framework 存密钥，profile 只存不透明
     `LlmAuthRef`；raw key 经一次性只写端点入 keychain，永不落 SQLite/日志/bundle；
     Flutter 不加 secure-storage 插件。刻意严于 genai(env)/aichat(config 明文)。
  7. **交付范围 = 后端优先**：本轮核心为中立性证明 + 安全存储 + probe + 标准化错误 + 诚实降级
     + ADR；最小设置 UI 后置为紧随其后的独立切片（Slice 3），不与后端基建挤同一交付。
- v2（2026-07-11）：评审发现 v1 是超载 phase（4 个协议 adapter + 设置 UI + 完整 judge
  留出评估挤在一个 phase）。judge 资格评估独立为 Phase 3.12.1；首批 adapter 收窄为
  **两个异构协议**证明厂商中立，其余协议为增量 slice，由 owner 按真实使用需求排期。
- v1（2026-07-11）：初版（含 judge qualification）。

## v3 切片与执行序（可执行粒度）

- **Slice 0 — 中立契约层（domain + application traits）**：`crates/domain/src/llm_provider.rs`
  （`LlmAdapterKind`、`LlmProviderProfile`、`LlmAuthRef`、`ProviderCapability`/`CapabilityClaim`、
  `LlmUse`、`DataRetentionPreference`、`CostBudget`、标准化 `LlmProviderError`），
  `crates/application/src/providers.rs` 增 `LlmChatAdapter` + `StructuredChatRequest/Response` +
  `SemanticRubricProvider`/`SemanticJudgeProvider` + `RubricDraft`/`JudgmentDraft`。
  错误类型不得携带 credential/header。
- **Slice 1 — 两 adapter + 同一 fake-server 契约套件（中立性证明，必做）**：新建
  `crates/llm-provider/`：`OpenAiChatAdapter` + `AnthropicMessagesAdapter` 实现 `LlmChatAdapter`；
  `LlmSemanticProvider<A>` 组合 prompt/schema/parse→draft；本地 axum fake server 对**两个 adapter**
  跑同一套场景：成功 / 拒绝 / schema-invalid / 截断 / 限流 / 超时。probe 结构化输出（Ollama 陷阱）。
- **Slice 2 — 安全存储 + registry + 诚实降级 use case**：`0036_llm_provider_profiles.sql`
  （只存 profile 元数据 + auth_ref，无密钥）；`SecretStore` trait + security-framework 实现；
  `AppServices` 注册 + `generate_rubric_via_provider` / `judge_attempt_via_provider` use case
  （过 3.11 validator；错误/拒绝/截断一律不写 judgment）；标准化错误 → HTTP 映射。
- **Slice 3（后置）— 最小设置 UI**：选 provider/用途/数据去向警告/连通与能力测试/费用上限。
- **Slice 4（增量，owner 按需）— OpenAI Responses / Gemini native**：只加 adapter，不改领域契约。
- **Slice 5 — ADR（provider 中立/密钥隐私/provenance/adapter 能力语义）+ closeout。**

## 外部参考与证伪（GitHub 对照，2026-07-12）

- **架构照 [rust-genai](https://github.com/jeremychone/rust-genai) 的中立-类型派**（自定义
  `ChatRequest`/`AdapterKind` + 每厂商 native 映射）；**拒绝 [LiteLLM](https://github.com/BerriAI/litellm)
  "全归一到 OpenAI wire format"** 作内部契约（踩 final §7.1 红线）。
- **能力描述符借 LiteLLM 分类学**（`supports_response_schema` 等），**但加 probe**：
  远端（genai/LiteLLM）对已知模型只静态声明，对自定义 endpoint 无一真探测——我们对 Ollama
  必须实测结构化输出。
- **密钥不照抄任何参考**：genai=env var、aichat=config.yaml 明文、terminal-aichat=密文旁放密钥；
  无一用 OS keychain。我们的 keychain+auth_ref 刻意更严。
- **结构化输出天生不统一**：genai 需特判 Gemini/Vertex 拒绝的 JSON-Schema 关键字——佐证
  §7.4「严格 JSON fallback + schema 校验重试，或声明不支持」。

## Objective

为 rubric generation、semantic judgment 与 writing feedback 建立厂商中立 provider 系统：
application 层任务契约 + 协议 adapter + 安全密钥存储 + 诚实降级。用两个结构不同的协议
adapter 通过同一契约测试，证明领域层未被任何厂商 wire format 绑架。

## User Value

用户可以选择自己的云端或本地模型服务，不被锁定在单一厂商；没有 API、费用预算耗尽或
provider 失败时，阅读、录音、复述和写作仍可继续，只失去自动语义反馈。

## Scope

### In

- Application traits：`SemanticRubricProvider`、`SemanticJudgeProvider`、
  `WritingFeedbackProvider`；输入输出只使用 3.11 contract。
- `LlmProviderProfile`：adapter kind/protocol version、base URL、model ID、认证引用、
  timeout、retry、并发/费用预算、retention preference、允许用途。
- Capability descriptor/probe：structured output、streaming、multilingual、audio/context、
  refusal、model-version provenance。协议兼容不等于能力等价，能力必须声明或探测。
- **Slice 1（中立性证明，必做）**：两个异构协议 adapter 通过同一 fake-server/contract
  suite——
  - OpenAI Chat Completions-compatible（自定义 base URL，覆盖多数兼容云服务与本地服务）；
  - Anthropic Messages（结构不同的 content block 协议）。
- **Slice 2（增量协议，owner 按需排期，可推迟到有真实使用需求）**：OpenAI Responses、
  Gemini native content/interaction API；新增 adapter 只加 adapter 与 descriptor，
  不改 3.11 领域契约（这是 Slice 1 必须证明的性质）。
- 密钥走 macOS Keychain 或等价 secure store；日志、SQLite、bundle 全链路脱敏。
- 设置 UI（最小面）：选择 provider、用途、数据离开设备范围、连通性/能力测试与费用上限；
  自定义 endpoint 显示数据去向警告。
- 标准化错误：offline、auth、rate limit、timeout、refusal、truncated、schema invalid、
  unsupported capability。

### Out

- **LLM-judge 资格评估（开发集/留出集、人工 gold、资格裁决）→ Phase 3.12.1**。
  本 phase 产出的判定一律停留在"未获显示资格"状态，不进入任何学习 surface。
- 自动写 reading/speaking/writing capability projection。
- 通用聊天机器人、agent tools、web search、RAG 或平台内置工具使用。
- 托管 LLPlayerNext 代理服务、账号计费与跨设备密钥同步。
- 将 OpenAI-compatible 误认为完整能力兼容；每个能力必须 probe/declare。

## Dependencies

- 3.11 Rubric/Judgment/Adjudication contract 与 persistence。
- 网络 API 是可选能力；本地核心路径不得成为其依赖。

## Key Work

1. Slice 1：traits + 两个异构 adapter 过同一 fake-server/contract suite（成功、拒绝、
   schema invalid、截断、限流、超时全覆盖），证明 trait 非单厂商形状。
2. Secure profile storage 与设置/连通测试；自定义 endpoint 显示数据去向警告。
3. 结构化输出 parser/validator、有限重试、费用/usage 记录、取消和超时。
4. Capability descriptor/probe 机制与 per-provider 差异记录。
5. 新 ADR：provider neutrality、secrets/privacy、provenance、adapter capability semantics。
6. （Slice 2，可推迟）OpenAI Responses 与 Gemini native adapter 增量接入。

## Exit Signal

- 两个异构协议 adapter 通过同一 rubric/judgment contract smoke；协议特有限制有明确
  descriptor；新增第三个 adapter 被证明只需 adapter 层工作量（Slice 2 落地或以
  设计评审形式确认）。
- 切换 provider 后领域 JSON 与 UI 语义不变；历史 judgment 仍指向原 provider/model/prompt。
- 删除/禁用 API key 后核心任务可用，自动反馈诚实降级。
- 密钥全链路不落普通存储；错误信息不回显 credential/header。

## Guardrails

- Flutter 与 HTTP route 不拼 prompt、不持有厂商 SDK 类型。
- 密钥不落普通数据库或日志；错误信息不得回显 credential/header。
- 不保存供应商隐藏 reasoning；只保存产品需要的结构化断言和公开 provenance。
- `source_kind=llm_judgment` 与 validation class 分离。
- 本 phase 完成不授予任何 judge 显示/证据资格；资格语义全部在 3.12.1 裁决。
