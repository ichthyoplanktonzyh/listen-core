# Phase 3.17 Closeout: Four-channel Projection & Cross-modal Review

状态：CODE COMPLETE — OWNER MANUAL QA PENDING

日期：2026-07-18

起点：`main@5bf8fbd3`

## Outcome

Phase 3.17 已把 lexical capability 的长期 authority 收口为四通道独立 evidence audit → versioned
proposal → explicit confirmation → projection。算法和 observation append 不再直接写 projection；
拒绝只追加 decision，确认在单事务内追加 decision、更新 projection 和 history，user override 始终
保持 read-time 优先。

Writing 在真实 lexical-target confirmation 缺失时明确返回 `insufficient_evidence` 并保持
`unassessed`，没有为了填满四通道借用 Speaking、Personal Expression、LLM 或 corpus 事实。

## Delivered

- SQLite schema v44：append-only `projection_proposals` / `projection_decisions`，含 update/delete
  trigger；旧库 additive migration，无静默回填或改写历史。
- Reading v1、Listening v2、Speaking v1 独立纯函数与 Writing unassessed qualification；算法版本
  变化新增并 supersede proposal，不改写旧 proposal、evidence、attempt、rubric 或 judgment。
- 唯一 confirmation repository gate；rejection、override priority、capability history 和全语言 rebuild。
- Cross-modal gap read model 只消费已评估状态，`unassessed` 不当失败；每项保留 observation/source
  reference 与 immutable snapshot，来源丢失仍可诚实解释。
- OpenAPI、Rust HTTP route、typed Dart DTO/client；词典资产层提供跨通道队列、proposal 确认/拒绝、
  来源快照、目标详情跳转及返回队列。
- ADR 0024、代码库架构/数据模型/测试说明与 owner manual QA 清单。

## Boundary Protection

- `UserSentencePattern` 仍是用户显式 durable asset，不是 projection、canonical construction 或
  capability truth；Speaking/Writing attempts 仍分通道、不可变。
- LLM judgment 只可能是可纠正 supporting evidence，本版本不把它作为 decisive evidence，更不能
  单独产生 acquired。Embedding、production corpus、Hunting List 均无 proposal/projection writer。
- 未引入通用 `FocusTarget`，未重开被拒绝的三 consumer 泛化；Personal Expression 不是第五 Studio。
- 未执行或冒充 3.15.7/3.15.8/3.15.9/3.16 或本阶段 owner QA。

## Automated Verification

- `scripts/test.sh --full --strict --low-memory --json`：6 checks passed，0 failed，1 skipped；
  Cargo 684 tests、Flutter 453 tests、strict Clippy/analyze、format 与 contracts 全绿。
- focused domain/persistence projection tests、v44 migration tests、legacy writer regression tests 全绿。

此前一次中间 Rust workspace 运行中的既有 subtitle import integration test 偶发失败，立即隔离复跑
通过，最终严格全仓入口也通过；按 phase 边界未修改无关 flaky。

## Remaining Owner Boundary

Owner 按 `3.17-MANUAL-QA.md` 使用真实数据库验证 proposal 来源、确认/拒绝、override、rebuild、
重启持久性与跨通道队列。完成前不得把本阶段标记为 owner accepted。
