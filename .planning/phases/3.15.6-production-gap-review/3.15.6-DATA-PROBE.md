# Phase 3.15.6 Real Small-N Data Probe

日期：2026-07-16

## 方法

- 来源：owner Application Support SQLite，原库 schema v38。
- 安全：复制到 `/tmp`，由当前 `SqliteRepository` 在副本上迁移至 v39，再调用 production corpus
  reindex；未打开、迁移或写入 owner 原库。
- 权威边界：reindex 只从 append-only `semantic_task_attempts` 派生 v39 projection。

## 结果

| 项 | 数量 |
|---|---:|
| semantic attempts | 6 |
| Writing summary attempts | 2 |
| 去重后 production documents | 1 |
| production tokens | 1 |
| distinct production lemmas | 1 |
| receptive acquired lexical entries | 1 |
| recognition evidence contexts | 0 |

Writing 的两个 revision 文本相同，因此 3.15.5 正确去重为一个 document；唯一 production
lemma 为 `nihao`。接收侧唯一 acquired lemma 为 `enjoy`，来源是 legacy status migration。

## 裁决

1. 当前 N 只足以验证 projection rebuild、exact lemma absence join 与 starter 文案。
2. 单一候选无法校准权重，也不能验证多个候选的经验排序；排序必须由透明、可测试的参照系
   定义，待真实 N 增大后再复核，而不是为了小样本调参。
3. 空 corpus 不返回 target；starter 可给练习候选，但 UI 明示不是能力结论。
4. ECDICT 的 BNC rank 是当前已安装、可追溯的通用频率参照；无 rank 时诚实 unavailable。
5. spoken corpus 当前为零且没有 writer，3.15.6 不提前模拟 3.15.7 数据形状。
