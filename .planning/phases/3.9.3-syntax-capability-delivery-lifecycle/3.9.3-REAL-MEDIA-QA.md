# Phase 3.9.3 Real-media and Lifecycle QA

日期：2026-07-13  
平台：macOS arm64，release `api-http`，隔离 HOME/SQLite

## Resource and install

- clean install 从 `not_installed` 到 `ready`，实际目录 142,969,731 bytes；基础 bundle 中
  Python/runtime/model 为 0 bytes。
- persisted state 在 backend restart 后仍为 ready；相同 track fingerprint 的 cache 直接复用。
- 删除模型目录后 validate 为 `partial` 且 disabled；恢复后 enable/probe 回到 ready。
- 旧 sidecar delivery validate 为 `stale`；Update 重新安装 `jsonl-v2` 后 ready。
- Disable 关闭 sidecar，track status 为 unavailable；Enable 重新校验后恢复。
- Update 中 Cancel 终止完整 installer process group，`.installing` 无残留，既有安装保持可恢复。
- Uninstall 删除 capability 目录与可重建 cache，字幕轨道/播放/基础学习数据保持存在。

## Real track

输入为 owner 本地 244 cue / 1773 word 新闻媒体与匹配 SRT。报告只保存输入 hash、cue index 和统计，
不复制字幕正文。

| Check | Result |
|---|---:|
| lexical mapping coverage | 100% |
| exact span | 98.759% |
| silent mismatch | 0 |
| deterministic refresh | true |
| isolated invalid tree | 1 cue |
| App first force rebuild | 2.10 s |
| App hot fingerprint hit | 0.11 s |
| App sentence result | 243 analyzed / 1 fallback |
| resident sidecar RSS observed | 159,488 KiB |

前导空格 cue 曾让 spaCy 生成 SPACE parser token 并造成 7 个额外坏树；`jsonl-v2` 现保留原 char
offset、过滤 SPACE token 并重映射 dependency heads。最终只剩原有 cue 147 双 root，以
`invalid_sentence` 单句隔离，整轨状态诚实为 partial。

播放响应性不等待 syntax：分析通过后台 HTTP task 触发，未安装/disabled 路径不启动 Python、
不弹窗，也不改变字幕导入与基础 consumer fallback。
