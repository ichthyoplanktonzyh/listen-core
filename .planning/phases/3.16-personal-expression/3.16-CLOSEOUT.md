# Phase 3.16 Personal Expression & Sentence Patterns — Code Complete Closeout

状态：✅ CODE COMPLETE — OWNER MANUAL QA PENDING  
日期：2026-07-18  
起点：`main@c86a5952`  
分支：`codex/phase-3.16-personal-expression`

## Delivered

- 按 main 实际底盘重审旧 PLAN，并以三 consumer razor 拒绝通用 `FocusTarget`：不迁移
  Hunting List，不用 3.17 的未来 consumer 为提前泛化背书。
- 新增 durable `UserSentencePattern` 资产、不可变 source snapshot、append-only version、slots
  与可空 system construction ref；SQLite v43 的 source navigation id 无媒体/字幕 FK cascade。
- 新增窄 repository/use cases、CRUD/search/version/history/export HTTP API、OpenAPI 与 typed Flutter
  client；资产显式删除才级联自己的版本与 attempts。
- Reading 句内显式收藏和“我的表达”资产旅程贯通；支持搜索、编辑、版本/来源/使用历史、
  JSON 导出与来源回听诚实降级。该 surface 不进入 domain identity，也不是第五 Studio。
- Writing 记录 typed learner response；Speaking 复用既有录音 → local ASR → corrected transcript →
  self-assessment 住户。两通道分别保存 immutable attempt，取消/未完成不制造 completed fact。
- 只读 3.17 handoff/export 暴露 pattern/version/attempt/channel/assistance/self-assessment/provenance；
  没有 observation、evidence projection、proposal 或 confirmation writer。

## Boundary Proof

- persistence negative test 在创建/练习前后核对 `learning_observations` 数量不变；writing payload
  不能携带 recording/raw speaking facts，speaking 必须引用 recording asset。
- 来源媒体 id/fingerprint 只是不可变快照中的可选导航 hint；删除或不存在的来源不影响资产、
  versions 或 attempts 存活。
- embedding/corpus hit 只能作为 `candidate_ref` provenance 进入显式创建请求；没有自动收藏、
  自动扩写或任何 learning writer。
- system construction ref 可空，并与用户拥有的 `pattern_text` 分字段保存；AI 文本没有 learner
  response 写入入口。

## Automated Verification

- `cargo fmt --all -- --check` PASS。
- `cargo check -p api-http` PASS。
- `cargo clippy --workspace --all-targets -- -D warnings` PASS。
- `cargo test --workspace --quiet` PASS。
- `./scripts/validate-contracts.sh` PASS（OpenAPI examples/contracts）。
- `flutter analyze` PASS；`flutter test` 451 PASS。
- `flutter build macos --debug` PASS；只有既有第三方 video_player deprecation/build-phase warnings。
- `git diff --check` PASS。

## Remaining Owner Boundary

由 owner 按 `3.16-MANUAL-QA.md` 使用真实媒体、麦克风、重启、来源删除与离线条件完成体验
验收。该清单尚未执行，因此本阶段不宣称 COMPLETE / owner accepted。

本 phase 未执行或冒充 3.15.7 realtime provider/device QA、3.15.8 embedding 首装/大语料 QA、
3.15.9 TTS 听感 QA，也未修改无关 flaky。
