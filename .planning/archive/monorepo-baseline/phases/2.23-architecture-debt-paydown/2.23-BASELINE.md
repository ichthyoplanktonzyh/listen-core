# Phase 2.23 Baseline Snapshot

> 记录时间：2026-07-02 23:38 CST
> 分支：`codex/2.23-handoff-tasks`
> 开工 HEAD：`eb1960d`（T7 后）

## 记录口径

- 本快照在 T1/T2/T6/T7 已完成后记录，用于 Phase 2.23 后续机械拆分的可量化基线。
- `./scripts/test.sh --quick --low-memory` 首次预检暴露既有 `cargo fmt` drift；本批先运行 workspace `cargo fmt` 修复格式，再记录转绿后的验证结果。
- 指标命令按 `2.23-HANDOFF-TASKS.md` T3 要求执行；测试计数使用 `cargo test -p <crate> -- --list` 与完整验证命令交叉确认。

## Code Size Metrics

| 指标 | 命令 | 基线值 |
| --- | --- | ---: |
| Desktop main file | `wc -l apps/desktop/lib/main.dart` | 3601 |
| Desktop `setState` usage | `rg -c "setState" apps/desktop/lib/main.dart` | 107 |
| Speech analysis implementation | `wc -l crates/speech-analysis/src/sound_analysis.rs` | 3383 |
| Dart timeline model | `wc -l apps/desktop/lib/models/timeline.dart` | 2596 |
| Persistence SQLite tests | `wc -l crates/persistence-sqlite/src/tests.rs` | 2603 |
| API HTTP tests | `wc -l crates/api-http/src/tests.rs` | 2024 |
| Flutter test files | `rg --files apps/desktop/test -g '*_test.dart' \| wc -l` | 25 |

## Rust Test Counts

| Crate | Count |
| --- | ---: |
| `api-events` | 3 |
| `api-http` | 40 |
| `application` | 46 |
| `diagnosis-core` | 20 |
| `dictionary-provider` | 8 |
| `domain` | 19 |
| `persistence-sqlite` | 46 |
| `speech-analysis` | 152 |
| `subtitle-core` | 24 |
| **Total** | **358** |

## Flutter Test Count

| Command | Result |
| --- | --- |
| `cd apps/desktop && flutter test` | 158 passed |

## Validation Results

| Command | Result | Notes |
| --- | --- | --- |
| `./scripts/test.sh --quick --low-memory` | PASS | 4/7 passed, 3 skipped; `cargo fmt`, `cargo clippy`, `cargo test (lib)` and `flutter analyze` passed. Rust lib tests: 325 passed, 0 failed. Real time: 733s. |
| `cargo test --workspace --quiet` | PASS | 358 passed, 0 failed. |
| `./scripts/validate-contracts.sh` | PASS | Python contract suites ran 14 + 1 + 2 tests OK; validated player, event, and OpenAPI contracts with 5 examples. Python 3.14 emitted existing `jieba` SyntaxWarning only. |
| `cd apps/desktop && flutter analyze && flutter test` | PASS | `flutter analyze`: no issues found. `flutter test`: 158 passed. |

## Baseline Repair Included In This Batch

The T3 baseline worktree includes a pure `cargo fmt` repair in four Rust files:

- `crates/api-http/src/sound_line.rs`
- `crates/api-http/src/tests.rs`
- `crates/application/src/tests.rs`
- `crates/diagnosis-core/src/lib.rs`

No behavior change was made in those files; the edits are formatter-only and were required before recording a green baseline.
