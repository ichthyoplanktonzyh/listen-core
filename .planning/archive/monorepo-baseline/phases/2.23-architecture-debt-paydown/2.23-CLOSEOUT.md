# Phase 2.23 Closeout: Architecture Debt Paydown

- 收口日期：2026-07-03
- 判定：**完成**。全部硬指标达成，真实媒体手工 smoke 通过（owner 于
  2026-07-03 确认），代码已合并至 `main`（merge commit `f7ae502`）。

## 目标回顾

在不改产品行为 / API contract / SQLite schema / 算法语义的前提下，偿还
2026-07-02 三轮架构审核（架构 / 数据模型与数据流 / 业务模型抽象）确认的
累积债务，作为 Phase 3.x 开工前的工程底座。

## 指标：基线 → 收口

| 指标 | 基线（2026-07-02） | 收口 | Gate |
|---|---|---|---|
| `main.dart` 行数 | 3601 | **1457** | ≤1500 ✓ |
| `main.dart` setState | 107 | **10**（均为局部 UI 状态） | ≤30 ✓ |
| `sound_analysis.rs` | 单文件 3383 行 | 14 模块文件，最大实现文件 398 行 | 无 >1500 ✓ |
| `persistence-sqlite/src/tests.rs` | 2534 行 | 8 文件按表域拆分 | 无 >1200 ✓ |
| `api-http/src/tests.rs` | 2021 行 | 8 文件按 route group 拆分 | 无 >1200 ✓ |
| `.planning/STATE.md` | 1149+ 行 | **162 行**（瘦身时点） | ≤400 ✓ |
| SQLite schema | v16（含僵尸表） | v17（`learning_resources` 已删） | ✓ |
| `cargo test --workspace` | ~350 | **358 passed** | 只增不减 ✓ |
| `flutter test` | 147 | **164 passed** | 只增不减 ✓ |

## 完成内容

### 审核缺陷修复（A-1..A-7，原审核会话）

诊断归一化接缝（"went"/"go"）、观察身份统一（`domain::lexical_observation_id`
单源，attempt 引用不再悬挂）、SSE 事件枚举双向 parity、LLTimeline 导入身份
重写所有权归一（`remap_lltimeline_identity` + 零残留不变量测试）、
`LexicalEntry` 双身份轴一致性校验、learning-loop 查询列/JSON 双表示写入收敛、
SSE payload 生产端 typed 化 + 跨语言 golden 契约。详见
`2.23-REVIEW-FINDINGS-REGISTER.md` A 节。

### Step 3：main.dart 收缩 + UI 状态单轨化（原审核会话）

六刀：status 单源化到 `PlayerController.status`；ResourceActions /
MediaSession / PlaybackActions 三个 context-free coordinator（`bind()` 注入
运行时钩子）；PlayerStage / SidePanel / PlaybackBar layout widget；settings /
媒体导入 / OpenSubtitles / manual review flow 函数。controller + Store 定调为
唯一 UI 状态模式并写入 `codebase/ARCHITECTURE.md`。切法实录见 `2.23-PLAN.md`
Step 3 回填。

### 交接任务包 T1-T9（交接执行人，Codex）

T1 删 `learning_resources` 僵尸表（v17）；T2 文档事实源修复（依赖图按 Cargo
复核、STATE 瘦身、MAINTENANCE 防复发规则）；T3 基线快照；T4 sound_analysis
模块拆分（provenance 字符串 multiset 验证不变）；T5 巨型测试文件拆分；
T6 零散 SSE payload typed 化（生产发射点 `json!` 清零）；T7 LLTimeline Dart
解析契约测试 + codegen 调研；T8 双家退役条件入档；T9 小项入档。
逐批验证记录见 register 第三至七批。

## 决策记录

- **ADR 0014**（2026-07-03）：Dart 模型解析保持手写，fixture 契约测试为
  防漂移标准机制；存量 `timeline.dart` 不做 codegen 迁移；3.x 新 DTO 默认
  手写 + 契约测试，新模型家族体量显著增长时可做仅新代码的试点（新 ADR）。
- AGENT.md 新增拆分触发规则：单实现文件 >1500 行或承载多子域必须先拆。
- controller + Store 为唯一 UI 状态模式（ARCHITECTURE.md Flutter 节）。

## 验证

- `cargo test --workspace` 358 passed、`./scripts/validate-contracts.sh` 通过、
  `flutter analyze` 无问题、`flutter test` 164 passed（合并后于分支尾部复验）。
- 真实媒体手工 smoke：按 `2.22-FRONTEND-E2E-QA.md` P0 路径，owner 2026-07-03
  确认无问题。

## 未尽事项与归属

- **C 类（归 3.x 主体工作）**：词组进诊断（C-1）、声音证据流入诊断（C-2，
  闭环核心兑现点）、provider 重归一化策略（C-3）、`LexicalEntry` 冗余投影
  字段删除（C-4，随 3.x API 演进）、词元/词形张力策略（C-5）、learning_loop
  纸面抽象逐切片验证（C-6）、per-event payload schema（C-7 余项）。
  见 register C 节。
- **D 类（明确 defer，有兜底）**：route manifest、speech-analysis crate 级
  拆分、逐功能 checklist 模板化。
- 本 phase 文件夹自此冻结；后续发现写入新 phase 文档。
