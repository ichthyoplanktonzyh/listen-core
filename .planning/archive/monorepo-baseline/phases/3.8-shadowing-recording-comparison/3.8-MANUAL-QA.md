# Phase 3.8 Shadowing & Recording Comparison — Real Media QA

状态：PASS（2026-07-12 09:07 CST；owner 明确确认全部通过）

目的：用真实英文媒体和真实麦克风完成“听 → 跟 → 录 → A/B/A → 重录 → 保存/删除”，
确认主观听感、系统权限与设备行为。自动测试已覆盖 typed contract、录音资产、非评分 completion、
客观比较算法和核心 UI 状态；本清单只保留必须由真实设备试听/观察的项目。

## 准备

- 解压并运行 `dist/listen-macos-arm64.zip` 中的 `listen.app`；包内已包含 Rust sidecar、ffmpeg、
  ffprobe 与 whisper runtime，无需另行启动服务。
- 从 `/Users/shadow/Desktop/视频` 选择一段带英文字幕的真实媒体；优先选 4–10 秒、含明显停顿、
  已有 ChunkTimeline 的句子。
- 准备一条没有 ChunkTimeline 的句子或暂时停用该资源，用于验证句级降级。
- 在安静环境使用 Mac 内置麦克风；如使用外接设备，记录设备名称。

## 主路径：当前媒体 chunk

- [x] 在精听练习浮窗选择“跟一下”；默认目标是当前韵律 chunk，不受语义 grouping mode 影响。
- [x] 原音以 0.9x 循环；切换 0.75x / 0.9x / 1.0x，听感与 UI 选中态一致，没有多路音频叠加。
- [x] 按“当前 chunk → 1 + 2 → 整句”逐步展开；文本、播放区间和循环边界与所选范围一致。
- [x] 点击录音后原音立即停止；首次授权时出现 macOS 麦克风权限请求，授权后能开始录制。
- [x] 停止录音后显示已保存状态、双波形、时长差、参考/录音停顿数及平均停顿偏移；不出现分数、
  “发音好/坏”或 speaking 已掌握之类结论。
- [x] 分别播放原音和录音，确认任一时刻只有一个发声源；点击 A/B/A，顺序确为原音 → 录音 → 原音。
- [x] 主观判断：双波形和时长/停顿数字与听感大体相符，可帮助发现明显拖长、抢拍或停顿错位，
  但没有把噪声当成确定诊断。
- [x] 重录一次，确认新录音与新比较替代当前展示，窗口无残留 busy/recording 状态。
- [x] 删除当前录音后展示清空；本地录音文件被移除，重新打开页面不再出现该资产。

## 跨媒体入口

- [x] 从复习卡的来源片段进入“跟一下”，使用独立切片播放器，不替换主播放器媒体与位置。
- [x] 从词典内联切片进入“跟一下”，录制、单独回放和 A/B/A 可完成；关闭练习后原工作台状态保留。
- [x] 跨媒体来源能解析到已注册媒体时保持可回放；只有 snapshot、没有 durable `mediaId` 的来源
  明确降级为媒体缺失，不影响录音与 prompt snapshot 保留。

## 降级与恢复

- [x] 拒绝麦克风权限：不开始录音、不抢占音频焦点，出现解释和“打开系统设置”入口；授权后重试成功。
- [x] 无 ChunkTimeline：入口仍可用，明确提示降级为整句 shadowing；无虚构 chunk 边界。
- [x] 录制完成前让来源媒体不可访问（移动/重命名测试副本）：录音与 prompt snapshot 仍保存，
  客观比较显示“录音已保存但比较不可用”，不会丢失录音或伪造比较结果。
- [x] 关闭正在录音的浮窗：录音取消，临时文件不作为资产落库；重新打开可正常录制。
- [x] 录制极短空白后停止：给出可理解错误，不创建空录音资产，下一次录制仍可开始。

## 证据与持久化核对

- [x] 完成一次 shadowing 后 attempt 为 `completed` 且 `score = null`。
- [x] 该完成不生成 speaking/listening observation，不生成 review item，不改变四通道 capability，
  不进入 content-fit 校准。
- [x] 删除来源媒体不级联删除已保存录音；录音仍保留语言、PCM16 WAV 格式、时长、SHA-256、
  来源范围与字幕 snapshot。

## 结果记录

### QA Round 1（2026-07-12）

- Owner 反馈：除以下 UX 问题外，其余已测试项目没有问题。
- 发现：从复习卡/词典内联进入跨媒体 shadowing 后，练习窗仍显示主字幕上一句/下一句按钮；
  点击会导航并播放主播放器内容。
- 修复：跨媒体练习固定为单一来源片段，隐藏句间导航与主字幕进度；播放/暂停及空格键改为控制
  切片播放器，并在导航函数入口阻断跨媒体调用。
- 自动回归：新增 widget test，锁定无左右按钮、无主字幕进度、播放按钮不触发导航。
- Owner 复验：复习卡入口、词典内联入口的播放/暂停与无左右切换全部通过。

- 媒体：owner 未记录具体名称
- 句子 / 时间范围：owner 未记录
- ChunkTimeline 来源：owner 未记录
- 麦克风 / 输出设备：owner 未记录
- macOS 版本：owner 未记录
- 0.75x / 0.9x / 1.0x：PASS
- A/B/A 与音频焦点：PASS
- 波形 / 时长 / 停顿可理解性：PASS
- 权限、无 chunk、媒体丢失降级：PASS
- 发现的问题：跨媒体练习窗主字幕导航泄漏；已修复并复验通过
- Owner 裁决：PASS（2026-07-12）

Phase 已按 owner PASS 裁决创建 `3.8-CLOSEOUT.md`；本文件与计划随 Phase 3.8 收口冻结。
