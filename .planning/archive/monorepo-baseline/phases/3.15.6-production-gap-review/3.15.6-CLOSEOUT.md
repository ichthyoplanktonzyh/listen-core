# Phase 3.15.6 Closeout: Cross-channel Production Gap Review v1

状态：CODE COMPLETE — OWNER GUI QA PENDING

日期：2026-07-16

## Delivered

- 只读 gap-(c) join：按 language/channel 聚合 reading/listening acquired profile、成功
  `LearningObservation` 与 `recognition_evidence` contexts，并排除已在 3.15.5 corpus 出现的 lemma。
- 透明 top-K：已安装 ECDICT BNC rank 分 band，随后按 evidence strength、recency band、精确
  timestamp 与 lemma 稳定排序；rank 缺失显式 unavailable。
- `empty/starter/ready`：空 corpus 零靶子；小 N 只显示练习候选，不宣称“不会用”。v1 readiness
  阈值为可见 heuristic（10 documents / 100 tokens / 20 lemmas），待更大真实 N 复核。
- `GET /v1/production-gap/review` + OpenAPI + typed Flutter client；听力词典资产页新增克制入口，
  靶子打开既有词汇详情/复习动作，不新增跨通道 ReviewCard 身份。
- 零 attempt/evidence/observation/capability/projection writer；查询不接触 TTS/playback lifecycle。

## Real Data Probe

owner 原库为 schema v38。仅复制到 `/tmp`，由当前 repository 在副本迁移 v39 并 reindex：
6 attempts、2 Writing summaries，去重后 1 document / 1 token / 1 lemma；接收侧 1 个
legacy-migrated acquired lemma、零 recognition context。真实 API smoke 返回 `starter`、top-1
`enjoy`、BNC rank 685，并解释 reading/listening profile 来源。完整记录见 `3.15.6-DATA-PROBE.md`。

## Verification

- `cargo test --workspace`：全绿（含 persistence 121；本次未触发已知 hunting 毫秒碰撞）。
- `cargo clippy --workspace --all-targets -- -D warnings`：通过。
- `flutter analyze --fatal-infos --fatal-warnings`：通过。
- Flutter 全量 443 项：通过。
- `./scripts/validate-contracts.sh`：OpenAPI、architecture guards 与 5 个 contract examples 通过。
- `git diff --check`：通过。

## Boundaries Preserved

- 3.15.5 projection 可整表删除重建；attempt 仍是唯一权威 learner-output 事实。
- 3.15.7 才能写 spoken corpus；provider transcript 与 factual assistance 不得被复盘改写。
- 3.15.8 才增加 embedding/近义 gap；3.16 template 有独立 durable identity/lifecycle。
- 任何 production capability proposal、确认与专项 cross-modal review 仍归 3.17。
- 3.15.9 TTS 仍是辅助 rendering/cache，零学习 writer。

## Deferred QA

owner 可在真实 App 中打开听力词典顶部 cross-channel 图标，确认中文 starter 文案、列表密度、
目标详情返回路径；这不阻塞 3.15.7 开工，也不冒充已完成的 GUI 体验验收。
