# Phase 3.15.9 Closeout: TTS Speech Synthesis

状态：CODE COMPLETE — OWNER GUI QA PENDING

日期：2026-07-16

## Delivered

- application 新增 provider-neutral `SpeechSynthesisProvider` port、provider/voice descriptor、
  synthesis request/output 与稳定错误语义；消费者不依赖平台命令或在线厂商协议。
- local-runtime 新增 `SpeechSynthesisManager`：输入校验、language/voice 选择、稳定 content hash、
  同 key single-flight、临时文件原子发布、cache stats/clear 与失败清理。
- macOS production composition 使用已安装 `/usr/bin/say` system voices，零下载、离线输出 AIFF；
  v1 不静默联网。local/remote locality 已在 seam 表达，remote adapter 留待显式凭据/隐私/成本 UX。
- HTTP/OpenAPI 新增 capability、synthesize、clear-cache；Dart 使用 typed model/client。
- Flutter 新增共享 `AuxiliaryAudioController`，统一 dictionary remote pronunciation 与本地 TTS；
  新播放先获取 audio focus，替换/stop/dispose 释放旧 decoder，录音/真实练习播放反向停止它。
- 词典标准音频存在时保持优先；缺失时才显示 synthetic pronunciation。真实 occurrence slice
  保持例句首选；个人 production corpus 与 Writing 可明确以 synthetic speech 朗读 learner-owned text。
- TTS manager 不持有 learning repository；零 attempt、observation、evidence、projection、review、
  production-corpus writer，未改变 3.15.5 authoritative attempts → rebuildable corpus projection 边界。

## Verification

- local-runtime 5 项：cache identity/hit/clear、same-key single-flight、失败零半文件、macOS voice
  parser、真实 system speech 非空离线 AIFF smoke。
- api-http 2 项：capability/synthesis/cache lifecycle 与 invalid request；OpenAPI path/schema drift 通过。
- Flutter 新增 controller focus/disposal、词典真实音频优先/synthetic fallback 与 API typed
  request/decode；全量 442 项通过。
- `flutter analyze` 干净；`./scripts/validate-contracts.sh` 通过；`cargo fmt --all` 通过。
- 已知 hunting 毫秒 event-id 119/120 flaky 属 phase 外噪声，本 phase 未修改该领域。

## Deferred

- Owner 按 `3.15.9-MANUAL-QA.md` 完成真实 GUI 听感/快速切换验证前，不宣称 owner accepted。
- Piper/其他 neural local adapter、Windows/Linux/mobile adapter 需独立质量、许可、体积与生命周期审计。
- Online TTS 需显式 provider 配置、发送文本同意、凭据/隐私/成本 surface 后接同一 seam。
- Conversation/review 批量素材、SSML、voice cloning、发音评测保留为后续 consumer/独立风险 phase。
