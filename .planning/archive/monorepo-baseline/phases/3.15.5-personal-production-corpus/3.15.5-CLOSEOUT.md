# Phase 3.15.5 Closeout: Personal Production Corpus

状态：COMPLETE

日期：2026-07-16

## Delivered

- 新增 schema v39：`production_corpus_documents`、lemma-keyed
  `production_corpus_entries` 与 trigger-maintained FTS5 companion。
- Writing typed attempt 正常落库后 best-effort 增量刷新对应 rubric；全量重建先派生全部行，
  再用单事务替换，派生或写入失败不会留下半套投影。
- 最终 learner response 按 rubric 去重摄入；完整回答只存一次，token occurrence 保存
  surface、lemma 与 Unicode-scalar span。
- 新增 exact lemma / phrase FTS 查询与手动 reindex HTTP API，并同步 OpenAPI。
- 词典详情新增「我的产出」：真实出现次数、回答文本、assistance provenance、revision，
  可打开原 attempt 的完整 revision 链；加载、空结果、不可用三态分离。
- `ProductionAssistance` 只记录事实来源，不以 scaffolding 布尔值冒充自治判断；spoken 仅有
  enum，无摄入 writer。
- 裁定 3.16 边界：语料是可重建事实投影，个人模板是显式用户资产；模板可引用语料 provenance，
  不复用投影身份或删除生命周期。

## Verification

- persistence：增量 lemma/phrase、零 evidence writer、幂等重建、单份回答存储、失败事务回滚。
- api-http：Writing attempt → 增量索引 → lemma/phrase 查询端到端。
- Flutter：客户端请求/typed decode、「我的产出」展示与 attempt callback、不可用/空态区分；
  相关词典回归 35 项通过。
- `./scripts/validate-contracts.sh` 通过；OpenAPI route/schema drift guard 通过。
- `cargo fmt --all`、Clippy workspace strict、focused Rust tests、v29/v30 旧库升级回归通过。
- `flutter analyze --fatal-infos --fatal-warnings` 干净；Flutter 全量 437 项通过。
- Rust workspace 全量复跑中，本 phase 与其迁移测试全部通过；唯一失败是既有 hunting check
  在两个事件落入同一毫秒时 ID 碰撞（119/120 persistence tests passed），该 exact test 随即
  单独复跑通过。本 phase 未修改 hunting/event identity，留给该领域独立修复。

## Deferred

- Spoken ingestion 等 3.15.7 的真实 session/turn facts 落地后追加，不提前猜 schema writer。
- Gap 分布、assistance 权重与复盘叙事归 3.15.6。
- 个人模板的 identity、编辑和收藏生命周期归 3.16。
- 语义相似检索归 3.15.8；本 phase 只提供 exact lemma 与 phrase FTS。
