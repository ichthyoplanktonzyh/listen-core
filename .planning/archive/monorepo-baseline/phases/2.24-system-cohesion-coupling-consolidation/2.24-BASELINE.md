# Phase 2.24 Baseline

> 快照时间：2026-07-14 15:58 CST
> 基线提交：`66ba51a0`

本文件记录建档时的耦合指标。开工后 Step 0 必须重新运行并记录最新数字；以下数字不是
允许回退的目标值，只是衡量 interface 收敛的起点。

## Rust

| 指标 | 基线 |
|---|---:|
| `application/src/lib.rs` | 1171 行 |
| `application/src/repositories.rs` | 848 行 |
| `AppServices` 依赖字段 | 21 |
| application 公开方法（文本扫描） | 约 169 |
| `speech-analysis/src/lib.rs` | 1184 行 |
| `speech-analysis` public implementation modules | 17 |
| `use domain::*` / `use crate::*` | 63 处 |
| `persistence-sqlite/src/subtitles.rs` | 1337 行；实现 4 个 repository trait |

宽 repository interface：

| Trait | 方法数 | 混合职责 |
|---|---:|---|
| `TimelineResourceRepository` | 30 | word/chunk/sense-group/phone timeline |
| `LearningAssetRepository` | 26 | lexical/capability/evidence/sense/import-export |
| `ReviewRepository` | 19 | review/hunting/recognition upgrade |

`api-http` 运行时编排热点：

| 文件 | 行数 |
|---|---:|
| `transcription.rs` | 1016 |
| `phonetic_analysis.rs` | 869 |
| `speech_jobs.rs` | 322 |
| `sound_line.rs` | 436 |
| `m18.rs` | 929 |

## Flutter

| 指标 | 基线 |
|---|---:|
| `LocalApi` 资源方法 | 约 147 |
| 公开返回 raw `Map` / `List<Map>` / `dynamic` 的方法 | 约 97 |
| `api_service.dart` 核心 | 263 行（已完成机械拆分，不是本 phase 问题） |

## Python

| 指标 | 基线 |
|---|---:|
| `timeline-production/production_pipeline.py` | 1433 行 |
| 主要职责 | acoustic cues、timeline conversion、report、audio prep、aligner orchestration、CLI |

## 已成立的守卫

- `api-http` 对 `speech_analysis::` 直接引用为 0。
- Cargo 依赖图单向，无循环依赖。
- Flutter `ApiTransport` 具有 production `HttpClient` 与测试 fake 两个 adapter。
- OpenAPI route parity、LLTimeline/事件 Flutter contract tests、workspace tests 已存在。

## Step 0 复测命令

```sh
git status --short
wc -l crates/application/src/lib.rs crates/application/src/repositories.rs \
  crates/speech-analysis/src/lib.rs crates/persistence-sqlite/src/subtitles.rs \
  scripts/timeline-production/production_pipeline.py
rg -n 'use domain::\*|use crate::\*' crates --glob '*.rs'
rg -n '^pub mod ' crates/speech-analysis/src/lib.rs
rg -n '^\s{2}(Future|Stream)' apps/desktop/lib/services/api \
  apps/desktop/lib/services/api_service.dart --glob '*.dart'
./scripts/test.sh --full --strict --low-memory
```

## Execution delta — 2026-07-14 16:28 CST

| 指标 | 基线 | 当前 checkpoint |
|---|---:|---:|
| speech public implementation modules | 17 | 0（4 个 curated capability modules + pronunciation root） |
| application legacy engine wrappers | 7 | 0 public leaks（6 删除，1 crate-private） |
| api-http runtime coordinator implementation files | 4 | 0（迁入 local-runtime） |
| fat repository interfaces | 3 / 75 methods | 0；拆为 12 个按不变量聚合的 ports |
| local-runtime adapter seams | 0 | 2（process/download，均有 production + fake） |

定向回归：speech-analysis 196 tests、application 59、local-runtime 9、api-http 43、
persistence-sqlite 110，合计 417 tests 通过。全量开工前基线结果为 Flutter analyze/test、
contracts 通过；cargo fmt/clippy 与一例 learning-loop 测试已有红项，详见
`2.24-INTERFACE-INVENTORY.md`。
